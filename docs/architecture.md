# Architecture

This document describes how DinnerOS is structured and why. It is the living
companion to the master project brief. When a decision changes, update the
[decision log](#decision-log).

## System overview

```text
┌───────────────────────┐       HTTPS / JSON        ┌──────────────────────────────┐
│  iOS app (SwiftUI)    │ ───────────────────────▶  │  DinnerOS API (Go, Heroku)   │
│  - Keychain tokens    │      /api/v1/...          │  modular monolith            │
│  - SwiftData cache    │ ◀───────────────────────  │                              │
└───────────────────────┘                           │  auth · users · households    │
         │                                          │  invitations · recipes       │
         │ Sign in with Apple / Google              │  ingredients · planning      │
         ▼                                          │  grocery · providers         │
┌───────────────────────┐   identity token verify   │  events · recommendations    │
│ Apple / Google IdP    │ ◀──────────────────────── │                              │
└───────────────────────┘                           └──────┬───────────┬───────────┘
                                                           │           │
                                              MongoDB Atlas│           │ seams (interfaces)
                                              (authoritative)          ▼
                                                           │   EmailProvider → Resend
                                                           │   GroceryProvider → Manual / Instacart / Walmart
                                                           │   RecommendationProvider → local → Autopilot (private)
                                                           ▼
                                                    ┌──────────────┐
                                                    │   MongoDB    │
                                                    └──────────────┘

┌─────────────────────────────┐
│ importers/hellofresh (CLI)  │ ── owner-only import ──▶ DinnerOS API
└─────────────────────────────┘
```

## Principles

1. **The backend is authoritative.** MongoDB holds the source of truth, and every
   authorization decision is made server-side. The iOS app caches data but never
   decides access.
2. **Modular monolith, not microservices.** One deployable, with clear package
   boundaries so a module can be extracted later. Autopilot is the only planned
   extraction.
3. **Deliberate seams only.** Interfaces exist where multiple implementations are
   *known* to be coming (table below). Everything else is concrete code.
4. **Deterministic core.** Grocery arithmetic, unit conversion, authorization, and
   constraint enforcement are plain, tested code. They never go through an LLM.
5. **Public code, private data.** The repository is public. Personal data, imported
   recipes, and proprietary Autopilot algorithms are not.
6. **The name is configuration.** "DinnerOS" is a working title (see README).

## Backend (`api/`)

```text
api/
├── cmd/server/          main: load config → build logger → router → graceful shutdown
├── cmd/sendreminders/   one push sweep and exit (Heroku Scheduler, hourly)
├── internal/
│   ├── config/          environment-driven configuration + validation
│   ├── httpapi/         router assembly, middleware, /health and /ready, route mounting
│   ├── platform/        domain-free infrastructure that domain packages may import
│   │   ├── httpx/       JSON responses, error envelope, strict request decoding
│   │   ├── logging/     slog construction; adds request ID from context
│   │   ├── mongodb/     client lifecycle, IndexSet management, error translation (mongotest/: test DB helper)
│   │   ├── ratelimit/   token-bucket middleware keyed by client IP or user
│   │   └── requestid/   request correlation ID in context
│   ├── auth/            provider token verification, DinnerOS sessions
│   ├── users/           User, AuthIdentity
│   ├── households/      Household, HouseholdMembership, roles/permissions
│   ├── invitations/     HouseholdInvitation, EmailProvider
│   ├── recipes/         Recipe, instructions, source metadata
│   ├── ratings/         per-member recipe ratings, household aggregates
│   ├── ingredients/     Ingredient, units, quantities, conversion
│   ├── planning/        Week plans, entries, week grocery list
│   ├── pantry/          PantryItem, default staples, pantry snapshot for grocery lists, purchases, usage estimates
│   ├── substitutes/     specialty ingredients: curated seed, store alternatives, house-made batches, household choices
│   ├── notifications/   household notifications and the push outbox
│   ├── push/            device tokens, APNs sender, the reminder sweep
│   ├── grocery/         aggregation engine, GroceryList
│   ├── providers/       GroceryProvider implementations
│   ├── events/          append-only behavior events, Recorder, client ingestion
│   ├── autopilot/       RecommendationProvider contract and generic types (stdlib only); baseline/: deterministic V1
│   └── recommendations/ Autopilot module: taste profile, week context, recipe attributes, proposals, provider adapter
├── pkg/                 domain-free reusable code (empty until justified)
└── migrations/          index + data migration conventions
```

### Module shape

Each domain package grows into the same small, boring layout as it is built:

```text
internal/<domain>/
├── model.go        domain types and invariants (no HTTP, no Mongo types leaking out)
├── service.go      use cases; depends on interfaces for storage and collaborators
├── store.go        repository interface
├── mongo_store.go  MongoDB implementation + index definitions
├── handler.go      HTTP handlers (decode → service → encode)
└── *_test.go
```

Dependency rules:

- `httpapi` depends on domain packages (to mount handlers). Domain packages never
  depend on `httpapi`. They use `platform/httpx` for responses and decoding.
- `platform/*` packages never import domain packages.
- Domain packages talk to each other through small service interfaces, never by
  reaching into another package's store or collections.
- `cmd/server` is the only place where concrete implementations are chosen and
  wired together. Commands that run outside the server (`cmd/sendreminders`,
  `cmd/importrecipes`) wire only what they use, the same way.

### Deliberate seams

| Interface | Implementations | Phase |
| --- | --- | --- |
| `AuthProvider` (`auth.IdentityVerifier`) | Apple, Google ✅ | 2 |
| `EmailProvider` | Resend, log-only (local/test) ✅ | 3 |
| `push.Sender` | APNs (token auth, HTTP/2) ✅; a fake in tests; none configured = logged no-op | 7 |
| `RecipeSourceImporter` | HelloFresh; later manual/import/partner | 5 |
| `GroceryProvider` | Manual; Instacart and Walmart via official APIs only | 8 |
| `RecommendationProvider` (`internal/autopilot`) | Local deterministic baseline ✅ → remote Autopilot | 10–12 |

### Cross-cutting concerns

- **Middleware order:** `RequestID` → `RequestLogger` → `Recoverer` →
  `MaxBodyBytes` → routes. A route that needs a different body limit replaces
  it with `httpx.OverrideBodyLimit`, placed after authorization. Wrapping the
  body in a second `http.MaxBytesReader` would enforce the smaller limit.
- **Logging:** `log/slog`, text locally and JSON in production. Each request
  produces one line with request ID, method, **route pattern**, status, bytes, and
  latency. The raw path and query string are never logged because they can
  contain tokens. `/health` and `/ready` log at debug level. Tokens, cookies, and
  credentials are never logged, and `Config` redacts connection-string passwords.
  Handlers log with `*Context` methods so the request ID is attached automatically.
- **Startup:** the API connects to MongoDB, pings it, and ensures indexes before
  listening. If that fails, it exits (fail fast). On SIGTERM it drains in-flight
  requests for up to 25 seconds.
- **Errors:** every non-2xx response uses `{"error": {"code", "message"}}`.
- **Limits:** request body size (`HTTP_MAX_BODY_BYTES`), server timeouts, and
  per-IP rate limiting on auth endpoints, invitation acceptance and preview
  (which share one limiter), and invitation creation, plus per-user rate
  limiting on event ingestion. Otherwise each has its own
  limiter instance. The recipe import route has its
  own body limit (`RECIPE_IMPORT_MAX_BYTES`) and extends the server's read and
  write deadlines for that request with `http.ResponseController`.
- **Household authorization:** routes under `/households/{householdId}` use
  `households.RequirePermission`, which returns 404 to non-members and 403 to
  members lacking the permission, and stores the membership in the request
  context for the handler and service.
- **Behavior events:** modules that observe household behavior record it
  through `events.Recorder` (implemented by `events.Service`), usually with
  `events.RecordOrLog`. Recording is best effort: a failure is logged and never
  fails the user's action. `cmd/server/behavior.go` wires the events and
  ratings modules.
- **Authentication:** domain handlers mount under `/api/v1` through
  `httpapi.Options.APIRoutes` and wrap protected routes in `auth.RequireAuth`,
  which puts the user ID in the request context (`auth.UserIDFromContext`).
- **Versioning:** product routes live under `/api/v1`. Operational routes
  (`/health`, `/ready`) are unversioned. So are browser-facing routes
  (`/invite` and `/.well-known/apple-app-site-association`, from
  `api/internal/applinks`), which mount at the root through
  `httpapi.Options.WebRoutes`.

## iOS (`ios/`)

```text
ios/DinnerOS/
├── App/          entry point, AppConfiguration, dependency container, AppDelegate (push token, tapped pushes)
├── Core/         networking (APIClient), auth session, Keychain, persistence (added as needed)
├── Features/     one folder per feature: Menu, Week, Recipes, Shop, Pantry, Household, Root
└── Resources/    asset catalog, string catalogs
```

- **SwiftUI + Observation** (`@Observable` models), async/await, `URLSession`.
- **Swift 6** language mode with `MainActor` default isolation. Networking and
  parsing run off the main actor explicitly.
- **SwiftData** is a local cache for offline reads and fast launch. It is never
  the source of truth. Server responses replace cached state.
- **Keychain** stores DinnerOS access and refresh tokens. No provider secrets or
  API keys ship in the app.
- **Configuration** flows from `Config/*.xcconfig` → `Info.plist` →
  `AppConfiguration`: display name, environment, API base URL, and Google iOS
  client ID. A missing API URL shows a configuration error screen and doesn't
  crash.
- **Dependencies:** `App/AppDependencies` is the only place that picks concrete
  implementations (`URLSessionTransport`, `KeychainTokenStore`). It injects
  `AuthSession` and `GoogleSignInService` through the SwiftUI environment.
- **Networking (`Core/Networking`):** `APIClient` is a `Sendable` struct over an
  injectable `HTTPTransport`. Its `@concurrent` methods encode, send, and decode
  off the main actor. Every non-2xx response becomes a typed `APIError` built from
  the `{error: {code, message, requestId}}` envelope.
- **Auth (`Core/Auth`):** `AuthSession` is `@Observable` and main-actor-isolated.
  It owns the tokens, restores the session at launch, and runs exactly one
  in-flight refresh that all callers share. Sign in with Apple, Google OAuth with
  PKCE through `ASWebAuthenticationSession`, and a Debug-only developer sign-in
  all end in `AuthSession.signIn`. See
  [authentication.md](authentication.md#ios-client).
- **Root flow:** `RootView` shows `restoring` (splash), then `SignInView`, the
  signed-in root, or the configuration error, based on `AuthSession.state`.
  `SignedInRootView` shows household onboarding until the user belongs to a
  household, then the tab shell.
- **Households (`Core/Households`):** `HouseholdsAPI` wraps the household and
  invitation endpoints. `HouseholdStore` (`@Observable`, main actor) loads the
  user's households after every sign-in, remembers the selected household per
  user in `UserDefaults`, and reloads from the server after each change.
  `HouseholdAccess` turns the response's `permissions` into which actions the UI
  shows. The API still authorizes every request. Invitation links arrive as
  universal links (`https://api.tlps.dev/invite#token=...`, through
  `onContinueUserActivity`) or custom-scheme links
  (`dinneros://invite?token=...`, through `onOpenURL`). The app previews the
  invitation and names the household and inviter before joining, and holds a
  link opened while signed out in memory until sign-in. Typed codes are
  previewed and confirmed the same way. Tokens and invite codes are never
  logged.
- **Planning (`Core/Planning`):** `PlansAPI` wraps the week plan and grocery
  list endpoints. `PlanStore` (`@Observable`, main actor, app lifetime) holds the
  week shown in the Week tab, which starts at this week in the household's time
  zone. Changes apply the plan the API returns; a delete, or a `403`/`404`/`409`,
  reloads the week. `GroceryListModel` backs one grocery list screen, keeps
  check-offs on the device, and exports the list as plain text. Checking off a
  line asks "Add to pantry?" in a card at the bottom of the list and records the
  purchase through `PantryPurchaseRecording` (#106). `PlanStore` owns the week the
  whole Menu tab shows, and hands every plan the server returns to
  `planDidChange`, which the Menu screen uses to patch its cards (#270, #271).
- **Menu (`Core/Menu`, `Features/Menu`):** `MenuAPI` wraps `GET .../menu`,
  `.../menu/recipes`, `.../menu/filters`, and `.../weeks`. `MenuStore`
  (`@Observable`, main actor, app lifetime) holds the selected week's sections,
  the week strip and how far back it reaches, the filter options, and the All
  Meals list (`MenuRecipeList`, one per screen, cursor-paged). It resets on
  sign-out and household switches, and `applyPlan` patches every card's `inPlan`
  state from the plan (#271). `MealPlanner` adds, steps servings, moves, and
  removes meals with an undo toast (#274, #275). Sections, badges, and
  `moreQuery` filters come from the server and are rendered generically (#272).
  `RecipeImageURL` asks the CDN for card-sized photos (#280).
- **Pantry usage (`Core/Pantry`):** `PantryItem` decodes `statusSource`,
  `lowThresholdPercent`, `unitSize`, and `estimate` leniently, so older
  responses and unreadable estimates still show the item. `PantryStore` records
  purchases (one `clientPurchaseId` per draft, so retries record once), loads an
  item's purchase history on demand, and reads and updates the household
  threshold. `PantryUsageFormat` builds short, localizable text from the
  estimate's numbers; the API's English `summary` is shown only in the item's
  detail. See [pantry-usage.md](pantry-usage.md#ios).
- **Notifications (`Core/Notifications`):** `NotificationsAPI` wraps the
  household notification endpoints. `NotificationStore` (`@Observable`, main
  actor, app lifetime) holds the unread count for the bell badge and the paged
  list, marks notifications read on screen before the request returns, and
  resets on sign-out and household switches (#108, #109).
- **Recipes (`Core/Recipes`, `Features/Recipes`):** the recipe screen is an
  edge-to-edge stretchy hero, the title and headline, a stats row (time,
  calories, protein, difficulty), tag chips, and Overview / Ingredients /
  Nutrition under a sticky picker, with Cooking Steps below and a bottom bar that
  adds the recipe to the week or steps its servings (#275, #277, #278).
  Customizations and pairings load per recipe and hide themselves when the API
  has none (#279). Summaries and details decode the menu's additive fields
  leniently (#281).
- **Ratings (`Core/Recipes`):** recipe summaries and details carry
  `householdRating` and `myRating`. `RatingDraft` enforces the API's rating
  rules while the user edits. `RecipeLibrary` saves or removes a rating, then
  reloads that recipe so the cache and the list row show the server's average.
- **Events (`Core/Events`):** `EventReporter` (`@Observable`, main actor, app
  lifetime) records `recipe.viewed`, `recipe.cooked`, `recipe.skipped`, and
  `grocery.item_checked` into a JSON queue in Application Support. It sends
  batches when the app goes to the background, every 60 seconds while active,
  and at 20 queued events. The queue belongs to one user and household.
- **Shopping (`Core/Shopping`, `Features/Shop`):** `ShoppingAPI` wraps the
  Phase 8a shopping endpoints. `ShoppingStore` (`@Observable`, main actor, app
  lifetime) backs the Shop tab: Walmart setup with an optional store number,
  the week's match ("Needs a Product", "Ready" with package steppers, "Not
  Included"), cart-link handoffs, and "Did you order these?", which the tab
  shell asks when the app returns from Walmart. Choose Product saves a product
  from a pasted Walmart link. The grocery list's toolbar opens the Shop tab for
  its week, and confirmed lines are checked off on the device (#200–#207).
- **Navigation:** a tab shell (Menu, Shop, Pantry, Household) with
  `NavigationStack` per tab, native sheets, and forms. The Menu tab holds the
  week strip, Your Meals, the server's carousels, All Meals, and a sticky bottom
  bar to the grocery list; the day-by-day week is the same `WeekView` behind the
  toolbar's **By Day** control (#270). Notifications open from a bell in the
  Pantry and Menu toolbars (#108).
- **Quality bar:** Dynamic Type, VoiceOver labels, dark mode, and explicit
  loading, empty, and error states for every screen.
- **Visual identity:** Apple-native structure with DinnerOS's own herb-green
  accent (`AccentColor` in the asset catalog, with a lighter dark-mode variant).
  No HelloFresh visual cloning.

The Xcode project uses file-system-synchronized folders (Xcode 16+). Adding a
Swift file to `ios/DinnerOS/` or `ios/DinnerOSTests/` needs no project-file edits,
which keeps `project.pbxproj` small and merge-friendly.

## Autopilot boundary

DinnerOS calls recommendations only through `RecommendationProvider`. The first
implementation is local and deterministic. When the engine proves useful, its
proprietary logic moves to a private service. DinnerOS then becomes tenant #1 of
a versioned Autopilot API. See [autopilot.md](autopilot.md).

`internal/autopilot` holds only the interface and generic types and imports
nothing from DinnerOS. `internal/recommendations` is the DinnerOS side: it owns
preferences and proposals and translates recipes, ratings, plans, and events
into the provider's inputs.

Learning from feedback lives in the provider (`internal/autopilot/baseline`),
recomputed from the interactions DinnerOS sends, and context arrives as a
generic typed signal map (`autopilot.Signals`). DinnerOS computes season,
holidays, and order date; the phone derives calendar and weather signals and
sends only those values.

## Decision log

| # | Decision | Rationale |
| --- | --- | --- |
| 1 | Go modular monolith with `chi` + `log/slog` | Small, idiomatic, no framework lock-in; stdlib-compatible handlers |
| 2 | MongoDB Atlas as authoritative store | Brief requirement. Indexes are designed per collection, not left to collection scans. |
| 3 | Official MongoDB Go driver v2 | Current supported driver |
| 4 | Hand-maintained Xcode project with synchronized folders (no XcodeGen/Tuist) | No extra tool to install. Modern Xcode removes most project-file churn. |
| 5 | iOS 18.0 minimum, Swift 6, Swift Testing | Modern SwiftUI (`Tab`, `@Entry`, Observation, SwiftData) for a private TestFlight audience; easy to raise |
| 6 | iPhone-only initially (`TARGETED_DEVICE_FAMILY = 1`) | Focus. iPad can come later. |
| 7 | swift-format (bundled with Xcode) instead of SwiftLint | Zero install, deterministic formatting |
| 8 | staticcheck + go vet + gofmt instead of golangci-lint | Fewer moving parts. Revisit if more linters are needed. |
| 9 | Fastlane + App Store Connect API key + Xcode cloud-managed signing | No certificates or profiles in Git or a match repo. The key lives in GitHub Secrets. |
| 10 | Opaque DinnerOS tokens separate from provider identities | Multiple login methods per user; provider data never becomes our identity |
| 11 | No license yet | Owner has not chosen one |
| 12 | Shared HTTP/Mongo helpers live in `internal/platform/*` | Domain handlers need response conventions without importing the router package, which would create an import cycle |
| 13 | Heroku container deploy (`heroku.yml` + `api/Dockerfile`) | The same image is built in CI and locally, with no third-party monorepo buildpack. The runtime is non-root Alpine rather than distroless because Heroku launches `CMD` via `/bin/sh -c`; CI checks this. |
| 14 | No CORS middleware | The only client is the native iOS app, which doesn't need CORS. Add it when a browser client exists. |
| 15 | `github.com/golang-jwt/jwt/v5` for JWTs; JWKS fetching written in-house (`internal/auth/jwks.go`) | jwt/v5 is the de facto Go JWT library and supports a strict algorithm allowlist (`RS256` for providers, `HS256` for our tokens), which blocks algorithm-confusion attacks. We only need RSA keys from two fixed URLs, so a ~150-line client is simpler to audit than a JOSE suite (`lestrrat-go/jwx`) or `keyfunc`. It adds a 1h cache, refetches on unknown `kid` at most once a minute, and serves stale keys during a provider outage. |
| 16 | Opaque refresh tokens with rotation and family-wide reuse detection, no grace window | Only the SHA-256 hash is stored. Replaying a rotated or revoked token revokes the whole sign-in. Two concurrent refreshes with the same token count as reuse, so the iOS client must serialize refreshes. We'd rather force a sign-in than ignore a possible token theft. |
| 17 | Logout revokes the whole token family, not just one session | A family has only one live session, so normal logout behaves the same. But a stale client logging out with an older token still ends the sign-in instead of leaving the newer session alive. |
| 18 | In-memory per-IP token-bucket rate limit on `/auth/*` (`internal/platform/ratelimit`, `golang.org/x/time/rate` v0.12.0) | One dyno today, so no shared store is needed. The client IP is the *last* `X-Forwarded-For` entry, which Heroku's router appends and clients can't forge. Each dyno limits separately; move to a shared store if we scale out. x/time is pinned below v0.13 because newer releases require Go 1.26. |
| 19 | `POST /api/v1/auth/dev` gated by `APP_ENV=development` **and** `AUTH_DEV_LOGIN_ENABLED=true` | Simulator and integration testing need sessions without Apple or Google. Config validation refuses to start production with the flag set, and the route isn't mounted otherwise. |
| 20 | `IdentityVerifier` returns `users.VerifiedIdentity`; `auth` depends on `users`, not the reverse | This is the `AuthProvider` seam. `GET /me` lives in the auth handler because it needs the auth context, and putting it in `users` would create an import cycle. |
| 21 | Google sign-in on iOS uses OAuth 2.0 authorization code + PKCE through `ASWebAuthenticationSession`, not the GoogleSignIn SDK | We only need an ID token. The flow is about 200 lines of auditable code: authorize URL, `state` check, form-encoded token exchange. It avoids a third-party dependency, its transitive pods or packages, its privacy manifest, and its release schedule. It also keeps the "Apple frameworks only" rule. The trade-off is that we build the button and handle Google's endpoints ourselves. Both are stable, documented OAuth endpoints. |
| 22 | iOS token refresh is serialized on the main actor with one shared `Task` | The API revokes a sign-in when the same refresh token is used twice (decision 16). Main-actor state makes "check for an in-flight refresh, else start one" atomic without locks. A refresh takes one network round trip, so hopping through the main actor costs nothing noticeable. |
| 23 | Launch refresh failures sign out only when the server rejects the refresh token | If the app signed out on network errors, opening it offline would discard a refresh token that is still valid (60-day sliding). A `400`/`401` from `/auth/refresh` still clears the Keychain. |
| 24 | Developer sign-in compiled only into `DEBUG` builds and shown only when `AppEnvironment == development` | Simulator testing needs a session without Apple or Google. `#if DEBUG` keeps the code path out of Release binaries, on top of the server-side gating in decision 19. |
| 25 | Roles map to permissions in one table (`households.rolePermissions`); code asks `Role.Can`, and granting requires `Role.Covers` | New roles (viewer, shopper, child, guest) are a table row, not a hunt for `isAdmin` checks. `Covers` stops anyone from granting more access than they hold, without special-casing admin. |
| 26 | The last-admin guard uses a conditional `adminCount` decrement on the household, not a transaction | Two admins demoting each other at once can't both succeed. It works on standalone local MongoDB like the Phase 2 code, and crash drift only makes the guard stricter. |
| 27 | The last admin can't leave, even as the only member | This keeps "every household has an admin" unconditional and avoids orphaned households nobody can see. A lone user can rename the household; deleting households is a later feature. |
| 28 | `households.RequirePermission` middleware returns 404 to non-members and 403 to under-privileged members; services re-check permissions on the membership they're given | Hides household existence and loads the membership once per request. The service check keeps authorization correct for non-HTTP callers and is covered by unit tests. |
| 29 | Invitations have a link token and a separate short code, both stored only as SHA-256 hashes | The token (256 bits) stays in the email link. The code (50-bit Crockford base32, with O/I/L read as 0/1/1) can be read aloud or texted, and per-IP rate limiting on accept makes guessing it infeasible. Unsalted SHA-256 is fine at this entropy and allows lookup by hash. |
| 30 | Accepting an invitation doesn't require the signed-in email to match the invited address | Apple private relay addresses and people using a different account would otherwise be locked out. Holding the secret is the proof. |
| 31 | Accept marks the invitation used (conditionally) before creating the membership, and undoes that if joining fails | Two users racing with the same code can't both join. The same user retrying gets their membership back, but a used invitation can't be replayed to rejoin after leaving. |
| 32 | One pending invitation per household and email, enforced by a partial unique index on a `pending` flag | Re-inviting revokes the previous invitation. The index makes that hold under concurrency and also serves the pending list. |
| 33 | Email failures don't fail invitation creation (`emailDelivered: false`) | The admin already has the code, so a Resend outage shouldn't block inviting. |
| 34 | `EMAIL_PROVIDER=log` for development; production requires Resend. `time/tzdata` is embedded | Local development and tests never send email or need a key. The Alpine runtime image has no zoneinfo, so `time.LoadLocation` would reject every time zone without the embedded data. |
| 35 | The recipe import route replaces the body limit (`httpx.OverrideBodyLimit`, `RECIPE_IMPORT_MAX_BYTES`) and extends its own deadlines with `http.ResponseController`, rather than raising the global limit or server timeouts | Only one route legitimately receives 10–15 MB. Every other route keeps the 1 MB cap and 15s/30s timeouts, and the override runs after authorization, so anonymous clients never get the larger allowance. The initial bulk load uses `cmd/importrecipes`, which bypasses HTTP and Heroku's 30-second router timeout. |
| 36 | Recipe identity is `(householdId, source, sourceRecipeId)` plus `sourceAliases`; imports compare the merged recipe with the stored one and bulk-write only changes | Sources republish recipes under new IDs (weekly menu clones), so aliases prevent duplicates. Comparing first makes re-imports no-ops, and bulk writes keep a 1000-recipe import to a handful of round trips. |
| 37 | The ingredient catalog is global and keyed by normalized name; unmatched categories are stored as `other` with `categoryConfident: false` | Ingredients mean the same thing in every household, and grocery aggregation needs one ID per ingredient. Flagging instead of guessing keeps the core deterministic. |
| 38 | `recipes.Service` takes a household ID, not a membership | HTTP routes authorize with `households.RequirePermission` (`household.view`, `recipes.import`). The `importrecipes` command has no signed-in user; it's an operator tool with direct database access, and it checks that the household exists. |
| 39 | The iOS app hides household actions using the `permissions` in responses, plus a copy of the role table (`HouseholdAccess`) to decide which roles a user may grant | Responses list the caller's permissions but not other roles', and `Covers` needs both. The copy only picks what the UI offers: if it drifts from the server, the worst case is a hidden action or a `403`, never extra access. Roles the app doesn't know get no actions. |
| 40 | The selected household ID is stored in `UserDefaults`, keyed by user ID | It isn't a secret, so the Keychain isn't needed. Keying by user means another account on the same device starts fresh. A stale ID falls back to the first household. |
| 41 | Invitation links use a custom URL scheme (`APP_URL_SCHEME`, default `dinneros`) registered through `Info.plist`, not universal links | It matches the API's default `APP_INVITE_URL_BASE` and works without a hosted domain or associated-domains entitlement. The scheme is configuration, so universal links can replace it later. |
| 42 | Opening an invitation link asks before joining. A link opened while signed out waits in memory, not on disk, until sign-in | Joining shares your name with the household and switches your selected household, so a link from a stranger must not do that silently. The token is a secret, so it isn't persisted; if the app quits before sign-in, tap the link again. |
| 43 | `HouseholdStore` reloads from the server after every change, and after a `403`/`404`/`409`, instead of patching local state | The requests are few and small. The screen always matches the server, including after concurrent changes by other members. |
| 44 | The iOS recipe detail shows only the amounts the source authored for each serving size, formatted from the exact `quantity` string (`quantityValue` only as a fallback), and never scales them | Sources don't scale linearly (4 servings may be ¾ oz, not 1 oz), and the grocery engine works from the same authored amounts. A size with no authored amount shows none instead of an invented one. |
| 45 | `RecipeLibrary` is an app-lifetime, in-memory store for the current household's list and loaded recipes; any filter change restarts from the first page, and a generation counter drops responses for old filters or households | Returning from a recipe or another tab shows what was loaded without refetching. Cursors are only valid for the filters that produced them. Nothing personal is cached on disk, and switching households or signing out clears it. |
| 46 | `APIClient` percent-encodes query items with RFC 3986 unreserved characters only | `URLQueryItem`'s default encoding leaves `+`, which Go's query parser reads as a space, so a search for "salt+pepper" would silently change. |
| 47 | A week plan is one `weekly_plans` document per `(householdId, week)`, keyed by the ISO week string (`2026-W38`), with entries embedded and capped at 50 | One read renders a week. Zero-padded week strings sort in week order, so the unique index also serves range queries. ISO weeks involve no time zone. |
| 48 | Concurrent plan edits use atomic single-document updates (`$push` guarded by `status: draft` and `entries.49: {$exists: false}`, positional `$set`/`$unset`, `$pull`), not a version field with `409 conflict` | Members editing the same week never lose writes and never have to reload and retry. A plan created by two first writes at once hits the unique index, and the loser retries once. The cost: when a conditional update matches nothing, a follow-up read decides between `404`, `409 plan_finalized`, and `409 plan_full`. |
| 49 | Entries snapshot the recipe's name and image; recipe details and the grocery list always read the live recipe | Plans render without loading recipes. An entry whose recipe left the household, or no longer offers the entry's serving size, is listed in the grocery list's `skipped` instead of failing the list. |
| 50 | Entry servings must be one of the recipe's authored sizes, and the grocery list uses those authored amounts unscaled (`RecipeServings == TargetServings`) | Same reason as #44: sources don't scale linearly. Scaling from another size would invent amounts. |
| 51 | A finalized plan locks its entries (`409 plan_finalized`) until `PUT .../status` sets it back to `draft` | Finalized means the week's shopping is decided, so the list shouldn't change silently. Reopening is one deliberate call. |
| 52 | `GET /plans/{week}` returns an empty draft for unplanned weeks, and `GET /plans` returns every week in the range (at most 26), not only stored plans | Clients render a calendar without special-casing missing weeks, and reading never writes. |
| 53 | A `recipeId` in a plan request body that isn't in the household is `400 validation_failed`, not `404` | The path's household and week exist; the body is what's wrong. `404` stays reserved for the path (non-members, unknown entries). |
| 54 | The week grocery list is computed on request with `recipes.Service.GetMany` (two round trips for the whole week) and an empty pantry | Nothing is persisted until Phase 7 adds the pantry and saved lists with checked state. Batch loading keeps a full week's cost constant instead of two queries per entry. |
| 55 | A pantry has one item per ingredient: unique `{householdId, key}`, where `key` is the normalized name or the catalog key. `POST .../pantry` merges into an existing item instead of adding a second one | The grocery engine decides per ingredient, so two items ("1 lb flour" in stock, "flour" out) would contradict each other. A merge applies only the fields sent, so a retried add is harmless. Different units of one ingredient share one item and one amount. |
| 56 | Pantry status alone decides grocery status: `in_stock` → `inPantry`; `out` → `toBuy` even for staples; `low` → the engine default (`pantryHint` when every recipe flags a staple, else `toBuy`). `isStaple` and amounts don't affect it | "Out" is something the household knows, so "probably have it" would be wrong. `Aggregate` reads it through the optional `grocery.OutPantry` interface, so its signature doesn't change. "Low" still means some is at home. Comparing amounts needs unit conversion between pantry and recipes, so it's deferred. |
| 57 | `pantry.Service.GroceryPantry` registers each item under its catalog ingredient ID and under `name:<key>`, and resolves unlinked items against the catalog each time a list is built | These are the planner's two line keys (catalog ID, or `name:` for uncatalogued lines). Resolving at read time means an item added as free text still matches after a later import adds the ingredient to the catalog, with no backfill job. |
| 58 | `planning.Service` gets the pantry through an optional `PantrySource` (`WithPantry`), reports `pantryApplied`, and fails the list if the pantry can't be read | Planning doesn't import the pantry package and stays testable without it. A failed request is clearer than a list that silently ignores the pantry. |
| 59 | Pantry writes use a `version` field with conditional updates and up to 5 automatic retries, instead of atomic field updates (#48) or `409` to the client | A pantry item has cross-field rules (an `out` item has no amount; a unit needs a quantity) that separate field updates could break when combined. An attempt fails only when another write committed, so 5 members can change one item at once without an error. |
| 60 | `GET /api/v1/ingredients?q=` is global and needs only a signed-in user, not a household | The catalog is global (#37) and holds only ingredient names and categories, no household data. If user-written ingredients ever enter the catalog, scope search to households first. |
| 61 | Default staples are a fixed list with aliases ("Black Pepper" also matches "Pepper"), added only when the pantry has neither the name nor an alias | Staples must link to the catalog ingredients recipes use, or they won't match grocery lines. Never touching existing items keeps the call idempotent and respects the household's own choices. |
| 62 | Pantry lists aren't paginated, and a household holds at most 1000 items | A pantry is small by nature, like a household's invitations. The cap keeps the unpaginated list bounded. |
| 63 | The iOS `PlanStore` shows the plan a change returns instead of reloading, except for a delete (the `204` has no plan), which removes the entry at once and then reloads. A rejected change (`403`, `404`, `409`) reloads the shown week before the error appears | Adding several recipes costs one request each. A week finalized or changed by another member shows its real state as soon as a change fails, so a `plan_finalized` message comes with the lock already visible. Unlike `HouseholdStore` (#43), successful changes don't reload, because plan responses already include other members' edits (#48). |
| 64 | "This week" on iOS is the ISO week containing now in the household's time zone, falling back to the device's when the stored zone is unknown. Week labels format the week's UTC-midnight dates in UTC | Plan dates are in the household's time zone, so late Sunday in Denver must still be that week even though it's Monday in UTC. `ISOWeek` dates stay time-zone free, so labels never shift a day. |
| 65 | Grocery check-offs are stored on the device in `UserDefaults`, keyed by household and week, with one entry per `ingredientKey` | The API has no checked state until Phase 7's saved lists. Ingredient keys are what the list aggregates by, so a check survives amounts changing when recipes are added. Nothing personal is stored: only IDs. |
| 66 | The Add Recipes sheet searches with its own `RecipeLibrary` instance; its plus button adds at once with `preferredServings(householdDefault:)` and the day chosen at the top, and tapping a recipe opens the full form | Searching in the sheet mustn't change the Recipes tab's list. Planning a week means adding several recipes in a row, so the common case is one tap, and the serving size follows the same rule as the recipe screen. |
| 67 | Entries move between days with a "Move To…" context menu and the edit sheet's day picker, not drag and drop | Dragging rows between `List` sections is unreliable and hard to use with VoiceOver. A menu works the same for every input method. |
| 68 | A rating is one document per (household, recipe, member), upserted, and managing your own needs only `household.view` | Ratings are personal opinions, not household settings, so anyone who can see recipes can rate them. One rating per member keeps averages honest. The history of changes lives in `recipe.rated` events, which carry the previous score, rather than in extra rating documents. |
| 69 | Rating tags come from a fixed allowlist; free text goes only in a comment of at most 500 characters | The recommender needs a vocabulary it can rely on (`make-again`, `never-again`, `kid-favorite`, …). Adding a tag is a one-line change; interpreting arbitrary strings is not. `make-again` together with `never-again` is rejected as contradictory. |
| 70 | Recipe `householdRating` and `myRating` are joined per page from `recipe_ratings`, not stored on recipes | It costs one aggregation and one find per page on the unique index, and there are no counters to keep consistent without transactions. Denormalize if lists need to sort by rating. An unrated recipe has `average: null`, not 0. |
| 71 | Event recording is best effort and synchronous: `events.RecordOrLog` logs failures and never fails the user's action, using a context detached from the request with a 2-second deadline | A rating or import that succeeded must not return 500 because the history write failed. An occasional lost event is acceptable for a recommender; a failed user action is not. The detached context keeps a client disconnect from dropping the event, and writing inline avoids a background queue that could lose events on shutdown. Revisit with an outbox if events ever need to be exact. |
| 72 | Each event type has exactly one typed payload in a closed set (`events.Payload`); unknown payload fields are rejected | Autopilot features need stable shapes, and a closed set keeps free text and personal data out of the history. New types are additive. `recipe.unrated` was added to the starting list so removing a rating is visible in the history. |
| 73 | Clients may send only `recipe.viewed`, `recipe.cooked`, `recipe.skipped`, and `grocery.item_checked`, in batches of at most 100. Each event is validated on its own, and invalid ones are reported in the response | Server-observed facts (rated, planned, imported) must not be forgeable. Rejecting per event keeps one bad event from blocking an offline queue forever. User and household come from the membership, never the body, and recipe IDs are checked against the household in one query per batch. |
| 74 | Client events may carry a `clientEventId`, unique per (household, user) through a partial unique index | A retry after a lost response would otherwise count a meal as cooked twice. Events without an ID are never deduplicated. |
| 75 | Event ingestion is rate limited per user (`ratelimit.Limiter.MiddlewareBy`), after authentication and before the membership lookup | Household members often share one IP, so a per-IP limit would couple them. Unauthenticated requests are rejected before they use a budget. It's the same in-memory, per-dyno limiter as decision 18. |
| 76 | Events are kept indefinitely with no TTL. Client `occurredAt` is accepted from 30 days back to 5 minutes ahead | History is the recommender's raw material, and the volume per household is tiny. Retention and archival are a later decision. The window bounds clock skew and stale offline queues. |
| 77 | Planning records `recipe.planned` and `recipe.unplanned` through an optional recorder (`planning.Service.WithEvents`); `DeleteEntry` takes the acting user and reads the entry first only when a recorder is set | The delete returns the remaining plan, so the removed entry's recipe, day, and date must be read before it. The read and the delete aren't atomic, so a concurrent edit can leave the event's day stale, which is acceptable for behavioral history. Without a recorder nothing extra is read. Entry updates and status changes aren't recorded yet. |
| 78 | Invitation emails link to `https://api.tlps.dev/invite#token=…`, a universal link backed by a server-rendered landing page, instead of the custom scheme from #41. The app still accepts `dinneros://invite?token=` | A custom-scheme link does nothing on a phone without the app, and during private TestFlight testing most invitees don't have it yet. One https link opens the app when it's installed and otherwise explains how to get it. The token goes in the fragment because browsers never send fragments, while Heroku's router logs full paths including query strings. Emails already sent keep working. |
| 79 | `POST /api/v1/invitations/preview` requires no authentication and returns only the household name, inviter name, role, and expiry | The landing page and a signed-out app must name the household before anyone joins. Holding the secret is already enough to join (#30), so it's enough to see what you'd join. Preview reuses accept's lookup and returns the same `404 invitation_invalid` and `400` errors, so it can't distinguish expired from nonexistent invitations any more than accept can. It never uses the invitation up and exposes no IDs, emails, or members. |
| 80 | Preview and accept share one per-IP rate-limit bucket | Separate buckets would double the guesses per IP against the 50-bit code. The app's preview-then-accept costs two requests out of a burst of 10. |
| 81 | The landing page is rendered once at startup, with a hash-based CSP (`default-src 'none'`; script and style by SHA-256), `Cache-Control: no-store`, and `Referrer-Policy: no-referrer`. `apple-app-site-association` is built from `APPLE_TEAM_ID` and `APPLE_BUNDLE_ID` and isn't served until both are set | The page handles a secret, so nothing else may run on it or learn its URL, and hashes need no per-request nonce. An association file with a wrong app ID would silently break universal links, so an unconfigured API serves none. |
| 82 | The app previews every invitation, from a link or a typed code, before asking to join. An `invitation_invalid` preview shows "This invitation is no longer valid" instead of a Join button | People see which household and inviter they're joining. An invitation that can't work never offers an action that would fail. |
| 83 | The iOS pantry is a fifth tab (Recipes, Week, Shop, Pantry, Household), not a screen inside Household | Households update the pantry around cooking and shopping, so it belongs one tap away rather than under member and account settings. Five is the most an iPhone tab bar shows before "More", so the next top-level feature has to replace a tab or live inside one. |
| 84 | The iOS app loads the whole pantry once, then searches, filters by status, and groups by aisle on device. Changes apply the item the API returns; only a rejected change (`403`, `404`, `409`) or skipped default staples reload | The list is unpaginated and capped (#62), so local filtering is instant and sends no request per keystroke. The app sorts like the API (aisle, name, ID), so an applied item lands where a reload would put it. A rejection usually means another member changed the pantry. |
| 85 | iOS parses typed amounts into the API's reduced exact form before sending ("1 1/2" and "1½" → `"3/2"`), rejecting negative, zero, and malformed input | The form explains the problem inline instead of after a `400`. What's sent equals what the API stores, so an edit that only retypes an amount sends no PATCH. |
| 86 | Multi-select status changes use `POST .../pantry/bulk` in batches of 200. IDs returned in `missing` are dropped from the list and named in a summary, not reported as a failure | A status change after shopping shouldn't fail because another member deleted one item. Single-item swipes use PATCH. |
| 87 | Expiry text ("Expires in 3 days", "Expired") counts calendar days in the device's time zone, not the household's | `expiresOn` is a date without a time zone, and "today" means the day for the person holding the phone. |
| 88 | After saving or removing a rating, iOS reloads that one recipe and copies its `householdRating` and `myRating` into the list row, instead of computing the new average itself. If the reload fails, only `myRating` is patched in | The server rounds the average and counts other members' concurrent ratings, so a local calculation could disagree with it. One small GET keeps the detail screen and list in step. The rating is already saved when a reload fails, so the user still sees it, and the average catches up on the next load. |
| 89 | `RatingTag` is an open string type with a `known` allowlist. Choosing `make-again` or `never-again` clears the other, rather than showing an error | A tag added on the server later still decodes and survives a re-save. The UI never lets a user build a rating the API rejects, and the rule lives in `RatingDraft`, where it's unit tested. |
| 90 | The iOS event queue is one JSON file in Application Support (`event-queue.json`): written atomically after each change, readable after first unlock, excluded from backup, and capped at 500 events (the oldest are dropped). Not SwiftData or `UserDefaults` | The queue is small, append-mostly, and read once per launch, so a file is simpler than a store and keeps `UserDefaults` free of growing blobs. First-unlock protection lets a background flush read it. Backups must not restore events to another device, where they would be sent twice. The cap bounds a long offline stretch; the API would reject events that old anyway (#76). |
| 91 | A `200` removes the whole batch, including rejected events. `5xx`, `408`, and transport errors retry after 5s, doubling to 15 minutes; `429` starts at 30s. Other `4xx` responses discard the batch; `401` waits for the next trigger. Events older than 29 days are dropped before sending | Rejected events can never succeed (#73), and resending a batch the server refused gets the same answer, so either would block the queue forever. `clientEventId` makes resending a batch after an ambiguous failure safe (#74). The 30s start for `429` matches the refill of one batch every 10 seconds after a burst. 29 days leaves a day of margin under the API's 30-day window. |
| 92 | The queue records its user and household. It is discarded on sign-out, when the selected household changes, and when the user has no household. It isn't touched while the session restores at launch | Events are sent as the signed-in user to one household, so a queue sent after a switch would attribute behavior to the wrong household. Keeping the queue while the session restores keeps offline events from being lost on every launch. A launch that ends signed out keeps the file until the next sign-in, which discards it if the user or household differs. |
| 93 | "Mark as Cooked" and "Skip" on Week rows only record events, so every member can use them, in finalized weeks too. The row shows the outcome only in memory for this launch | Cooking happens after a week is finalized, and reporting it changes no plan, so `plan.edit` isn't needed. The API has no way to read events back, and storing outcomes on the device would create a second, unsynchronized record. A shared "cooked" state belongs in the plan API if households want one. |
| 94 | `recipe.viewed` needs 3 seconds on screen with the app active, then at most one per recipe per 10 minutes (in memory). `grocery.item_checked` is recorded for each individual check and uncheck, but not for "Uncheck All". Lines without a catalog ingredient send only `name` | A quick scroll past a recipe or returning to it isn't a new signal. Unchecking corrects a mistaken check, and the payload has `checked` for that, while "Uncheck All" resets the list and says nothing about shopping. `name:` keys aren't ingredient IDs, so they aren't sent as one. |
| 95 | Pantry usage is estimated per cycle (docs/pantry-usage.md): a purchase, or the first amount a person records, is 100%. Remaining = segment start − recipe use − learned rate × days, never below 0. The estimate is computed at read time and never stored | Storing a remaining amount would go stale every day and need a job to keep it current. Keeping the inputs (cycle, recipe use, rate) makes every number reproducible and explainable ("2 recipes used 6 tbsp, plus ~1 tbsp/day"). |
| 96 | Purchases are recorded explicitly with `POST .../pantry/purchases` and `source` `grocery_list` or `manual`. `provider` is reserved for Phase 8 orders, which write the same record. Checking off a grocery line only records a purchase after the member confirms "Add to pantry?" | A check-off doesn't prove what or how much was bought, and `grocery.item_checked` events are best effort (#71). An explicit, idempotent (`clientPurchaseId`) purchase with the confirmed amount is the reliable starting point for a cycle. |
| 97 | A purchase assumes the previous stock was used up. Leftovers aren't carried into the new cycle. A person's correction, or marking the item out, replaces the assumption | Carrying an estimated remainder would compound estimate errors into the new 100%. Most households restock when an item is gone or nearly gone. The early-restock bias is documented, and a correction always wins. |
| 98 | Usage amounts convert exactly or not at all: volume↔volume, weight↔weight, a discrete unit only to itself or through an explicit `unitSize` from a purchase. A deduction that can't convert is skipped, recorded with a reason, and counted on the item | Guessing a density or "1 onion ≈ 8 oz" would make alerts wrong in ways nobody can see. A visible "1 recipe couldn't be counted" tells the household to correct the amount. |
| 99 | Cooking deducts through an `events.Listener` that runs after `recipe.cooked` is stored: synchronously, best effort, with a detached context bounded to 10 s. `pantry_cook_usage` has a unique `{householdId, sourceKey}` (`entry:<entryId>`, or `event:<userId>:<clientEventId>`), inserted before any item changes | "Mark as Cooked" only sends an event (#93), so the event is the one place to hook. The unique record makes re-marking an entry, client retries, and duplicate events deduct once. Inline, like #71, avoids a queue that could lose work on shutdown. Any member may cook, so no `pantry.edit` is needed. |
| 100 | A deduction for a serving size the recipe doesn't author is scaled linearly from the nearest authored size and recorded as `scaledFrom` | Unlike the grocery list (#50), a deduction is already an estimate. Skipping it would undercount use; the record shows it was scaled. |
| 101 | The learned non-recipe rate is the median daily use across the 6 most recent closed segments lasting at least a day, and needs 2 of them. Segments are embedded in the item (capped at 6), not a separate collection | The median resists a single bulk purchase or holiday. Two segments is the least that shows a pattern. The history is tiny and always read with the item, so embedding avoids a join and a second write. |
| 102 | Auto-low creates the `pantry.low` notification first (dedupe key `pantry.low:<itemId>:<cycleId>`, unique per household), then sets the item `low` with `statusSource: estimate` conditional on its version. At most one alert per item per cycle. A status set by a person since the segment started blocks auto-low | Notification first means a failed status write is retried by the next check without a second alert. People always win: an estimate never overrides what someone just said, and "it's fine" sticks for the cycle. |
| 103 | Time-based decay needs no scheduler: `GET .../pantry` and notification reads run the household's low-stock check first (bounded to 3 s for notifications, failures logged). Push will add an hourly Heroku Scheduler sweep calling the same idempotent `Refresh` | In-app alerts only matter when someone opens the app, and that request can do the check. Push must reach closed apps, which is when a sweep earns its cost. |
| 104 | Notifications are their own module (`internal/notifications`): household-wide, with per-member `readBy`, and the collection is the push outbox (`push.status` starts `pending`, partial index for pending) | Other features will notify too, so it isn't pantry-specific. A household sees one alert, but each member dismisses it separately. Writing the push state with the notification means the APNs worker needs no second outbox and no producer changes. |
| 105 | The low-stock threshold is a household setting (`pantry_settings`, default 80%, 1–100) with an optional per-item override, not an environment variable | Households differ, and so do items (running out of salt is not running out of milk). It's product data members change, not deployment configuration. |
| 106 | iOS asks "Add to pantry?" after a grocery check-off in a card inset at the bottom of the list, not a sheet or alert. Confirming closes it at once and records the purchase in the background; checking another line replaces an unanswered card; a failure shows Try Again, which resends the same `clientPurchaseId`. "Don't ask during this trip" is in memory on that list screen's model, so leaving the list, switching households, or signing out ends it | A shopper checks off many lines in a row, often one-handed, and a modal after every tap would block the list. Recording in the background keeps the checklist responsive, and one ID per prompt makes a retry after a lost response record a single purchase (#96). A trip is one visit to the list; a stored preference would silently stop prompts on the next shop. |
| 107 | The household low-stock threshold is edited from the Pantry tab's More menu ("Low-Stock Alerts…"), shown as percent used and read-only without `pantry.edit`. An item's own threshold is a toggle and stepper in its edit sheet, sent as an integer or `null` | The setting is guarded by `pantry.edit`, not `household.update`, and only changes pantry estimates, so it belongs beside the items it affects. Household settings stay about the household itself. |
| 108 | In-app notifications open from a bell toolbar button with an unread badge on the Pantry and Week screens, as a sheet with the paged list. There's no notifications tab or tab badge. Tapping `pantry.low` marks it read and pushes that item's detail inside the sheet | The tab bar is full at five tabs, and a sixth would hide a tab behind More. Pantry and Week are where low stock matters (restocking and planning). Pushing inside the sheet works from either tab without switching tabs or losing the member's place. |
| 109 | The unread count refreshes when a household is activated, when the app becomes active, after every pantry load or change (purchases and threshold changes included), after Mark as Cooked, and when the notifications sheet closes. There's no polling | Notifications are created only by pantry reads, changes, and cook deductions (#102, #103), so these are the moments the count can change. Polling costs requests and battery for alerts that aren't urgent until push exists. |
| 110 | Mark as Cooked sends the event queue right away, then refreshes the pantry and the unread count | The API deducts a recipe when its `recipe.cooked` event is stored (#99), and the queue otherwise waits up to 60 seconds (#91), so the pantry would show a stale estimate right after cooking. The send still honors backoff. |
| 111 | A low status the estimate set (`statusSource: estimate`) shows as an outlined, dashed "Estimated Low" pill with a trend icon; a person's status keeps the filled pill. Rows with an estimate show a small ring and "~N% left". The API's `summary`, recipe use, daily rate, the threshold and its source, and recent purchases are on the item's edit sheet, or a read-only detail for members without `pantry.edit`. Purchase history is loaded when that screen opens and isn't cached | Members need to tell at a glance which statuses a person confirmed and which are estimates they can correct (#102). Rows stay scannable and the explanation is one tap away. History is only read on that screen, so a cache would add invalidation without saving a visible request. |
| 112 | Specialty ingredients are their own module (`internal/substitutes`). Planning reaches it through an optional `SpecialtySource` and the pantry through an optional `KeyResolver`; substitutes imports the pantry, never the reverse | The pantry and planner keep working without it, and there's no import cycle. Choices change grocery lists and batches change the pantry, so neither module should own the other's rules. |
| 113 | The curated set is a versioned JSON seed (`internal/substitutes/seed`) embedded in the binary and synced on startup, guarded by a per-specialty content hash. Specialties the seed drops are retired, never deleted. `cmd/seedspecialties` previews or applies the same sync and reports catalog matches | Curated option IDs are part of the API contract (choices reference them), so the database must match the running binary, and Heroku runs no release phase. A hash-guarded sync writes nothing on a normal boot. Retiring keeps household choices resolvable. The command lets a seed change be checked against real data first. |
| 114 | "Is this a specialty ingredient?" is decided at read time from curated names and aliases against catalog keys and normalized recipe names, not stored on catalog ingredients. Unknown specialty-looking names aren't flagged | Same reason as #57: an ingredient the catalog learns later matches with no backfill, and the global catalog isn't written by another module. Guessing from words like "paste" would flag store products. |
| 115 | Curated defaults are suggestions: a list changes only after a member chooses (per ingredient, or all defaults at once with `POST .../choices/defaults`). `as_is` keeps the name and stops prompting. Households override curated options by adding their own (`basedOnOptionId`), not by editing them | Replacing what a recipe says without anyone deciding would surprise a household that buys the product elsewhere. One call still sets up a whole history. Read-only curated options stay comparable across households and safe to update in the seed. |
| 116 | A store alternative is an exact ratio: `per` of the specialty is its ingredients. A recipe amount converts to `per` exactly, through the specialty's estimated packet sizes for `count`/`package`. An amount that doesn't convert lists the ingredients without amounts | Consistent with #98: nothing is guessed. Packet sizes are the one estimate, stored as data where they can be corrected. An ingredient without an amount still reaches the shopping list. |
| 117 | A house-made batch is the pantry item keyed by the specialty's normalized name. Making one is a purchase with `source: house_made` through the existing restock (a new cycle at the yield, `expiresOn` from the shelf life), recorded only by `POST .../specialty-ingredients/{id}/batches` | The cook deduction already matches recipe lines to items by key, so batches deduct, estimate, and alert with no new pantry logic. One endpoint knows the yield, packet size, and shelf life, so apps can't record an inconsistent batch. |
| 118 | The week's grocery list keeps a batch as one `inPantry` line when it's in stock and its estimate covers the week's total (or the amounts can't be compared); otherwise it lists the ingredients for enough batches to cover the shortfall, at least one. Low, out, or missing always means making one | The household's question is "do I need to make it this week?". Trusting status when amounts can't be compared matches #56. Several batches for a big week avoids a second trip. |
| 119 | Grocery list changes are additive: every item gets `specialty`, `specialtyDetail`, and `via`; the list gets `specialtiesApplied` and `batches`. Item statuses are unchanged (no `toMake`), and provenance comes with English `text` | Existing clients keep decoding while the iOS work lands. The `batches` section carries "make" without overloading a shopping status, and ready text keeps explanations consistent across clients. |
| 120 | `grocery.ApplySpecialties` is pure and runs in planning before `Aggregate`; `Aggregate` only gains `Line.Via`, `Line.Specialty`, and `Line.Sources` | The engine rules ([grocery-engine.md](grocery-engine.md#rules): deterministic, no I/O) still hold, the replacement is unit tested in isolation, and ordinary lines aggregate exactly as before. |
| 121 | Reading specialty ingredients needs `household.view`; choosing, options, defaults, and batches need `pantry.edit` | Choices change what the household buys and batches change the pantry, which is what `pantry.edit` guards (#59). Members have it; viewers don't. |
| 122 | A specialty's recipe count comes from one aggregation (`recipes.Service.FindIngredientUse`) over the household's recipes by catalog ingredient ID, counting each recipe once per specialty | The setup screen needs "used in 52 recipes" without loading every recipe. Counting by ID covers aliases; recipe lines without a catalog ID aren't counted, and imports always link one. |
| 123 | `internal/autopilot` holds the provider interface and generic request/response types and imports only the standard library (a test enforces it and that no recipe source is named). `internal/autopilot/baseline` implements it; `internal/recommendations` is the DinnerOS module and adapter | Moving Autopilot to a private service then means replacing one constructor in `cmd/server`. The provider sees catalog items, ratings, and interactions, never recipes, plans, or where recipes came from, as autopilot.md requires. |
| 124 | Autopilot proposals are stored separately from plans (`autopilot_proposals`, one per household and week). Accepting adds the meals to the draft plan as entries with `origin: autopilot` and `proposalId`. Generating keeps existing entries (their days aren't planned), and a finalized week can't be generated, swapped, or accepted | A suggestion shouldn't change the grocery list or other members' week until someone accepts it, and `recipe.planned` should only describe real plans so conversion stays measurable. Explanations, swap history, and the model version live with the proposal. Regenerating never churns plan entries or their events. |
| 125 | Swap, accept, and reject send the proposal's `version` (`409 proposal_changed` when stale). Accept claims the proposal (status `accepted`) before one atomic `AddEntries`, and reopens it if the plan change fails. Meals whose day now has an entry or whose recipe is already in the week are skipped and reported, never replaced | Two members can act on one proposal. Claiming first means only one accept adds meals, without transactions. Skipping instead of overwriting keeps "never overwrite manual entries" true even when the plan changed after generating. |
| 126 | Hard constraints filter candidates before scoring: allergens, diets, excluded ingredients (word match with plurals), cuisines, proteins, tags, no spicy, any member's `never-again`, no serving sizes, recipes already in the week, and week or day `maxMinutes`. Unknown cook time never passes a cap. Business objectives are clamped to ±0.2 and apply only to candidates that passed | autopilot.md's layer precedence. A test gives a forbidden meal top ratings, a matching rule on every day, and the largest boost, and checks it's never planned or ranked. Unknown times failing a cap keeps "≤20 minutes" honest. |
| 127 | Effective cook time is `recipes.CookMinutes` = max(prepMinutes, totalMinutes), ignoring missing values; it's computed, not stored, and exposed as `cookMinutes` on recipe summaries and details. Time bands (quick ≤ 20, medium ≤ 35, long) are household settings; unknown time is medium | In a real imported catalog totals were often below prep (380 of 429 recipes had totals ≤ 20) and 48 had only prep, so neither field works alone. Computing it at read time needs no backfill when imports change. |
| 128 | The week is chosen by beam search (16 partial weeks, 40 candidates per day, most constrained day first) whose beam keeps the best week per cook-time and rule pattern, followed by deterministic local search (replace a meal, swap two days). Ties break with an FNV-1a seed of household, week, attempt, and model version | Plain greedy and a plain beam missed arrangements such as alternating long cooks, because the beam filled with weeks differing only by interchangeable meals. Both passes stay well under 100 ms for 500 recipes and are reproducible. |
| 129 | Weekday rules are soft, one per day. A meal matches every group the rule sets (proteins, methods, cuisines, tags), any value within a group. `every_week` prefers matches and penalizes non-matches that day; `at_most_once` allows one full match per week. `timeBand: long` means long cook OK (no weeknight limit, doesn't count against the long-meal allowance). Rule methods must be in the household's equipment | Covers "Taco Tuesday" and "Sunday smoker night, chicken or pork, not every night" with one generic model. Requiring equipment keeps rules consistent with what the household can cook. |
| 130 | Proteins, allergens, diets, spicy, and cooking methods are derived at read time by keyword rules in `recommendations/attributes.go`, returned with evidence by `GET .../autopilot/recipes/{id}/attributes`. Hard-constraint rules are conservative (evidence of a violation wins; a diet needs ingredient data or a tag). Households override methods per recipe (`autopilot_recipe_overrides`) | Recipes have no such fields. Explainable rules can be reviewed and tested; overrides fix the imperfect method heuristic (smoker-friendly cuts) without code changes. Being conservative only costs a candidate, never an allergen. |
| 131 | The taste profile is one document per household with seven sections. Each section records who changed it and when; `PUT` replaces the profile (onboarding), `PATCH` replaces the sections sent. Writes retry on version conflicts, and each change records `autopilot.preferences_updated` with a bounded diff summary; history is read from events | Preferences are first-class and editable screen by screen, with attribution. Events are already the append-only history Autopilot learns from, so a separate history collection would duplicate them. |
| 132 | Week context is its own document per household and ISO week: skip, busy, meals, a hard `maxMinutes`, servings, per-day overrides, and a note that V1 stores but doesn't interpret. `busy` is soft (no long meals, at least half quick); `maxMinutes` is hard | A week's circumstances differ from standing preferences and expire with the week. Keeping both a soft and a hard option lets "busy" still plan a full week while a strict cap explains shortfalls. |
| 133 | Generating over a pending proposal records `week.rejected` (`regenerated`) and softly avoids its meals and the meals swapped out of it (`avoidPrevious`, default true). The attempt number is part of the seed | Regenerate must give a different week while staying deterministic: the previous proposal is an input. It's a penalty, not a filter, so a small catalog still fills the week. |
| 134 | Swap calls `RankMeals` for the slot's day with the plan's entries and the other proposed meals as fixed, excluding the current meal and every meal already swapped out of that slot (at most 20) | The next best alternative honors the same constraints, variety, cook-time mix, and rule frequency, and repeated swaps walk down the list instead of cycling. |
| 135 | Implicit feedback is server-observed events: `week.generated`, `week.accepted`, `week.rejected`, `meal.swapped`, `meal.rejected`, and preference change events. Plan entries store `origin` and `proposalId`; `recipe.planned` carries `proposalId` and `recipe.unplanned` carries `origin`. V1 records but doesn't learn from them | These give the success metrics (acceptance with ≤ 1 swap, swap rate, conversion) and labels to calibrate weights later, while keeping V1 deterministic. Cooked and skipped events join to autopilot meals through `entryId` without the ingestion path knowing about plans. |
| 136 | Generation loads its inputs per request (main-meal catalog without steps or nutrition, all ratings, 26 weeks of plans, 52 weeks of cooked/skipped events, low pantry items) and caches nothing. `inputsHash` fingerprints the provider request | About 65 ms end to end locally for 500 recipes. A cache or precomputed features would add invalidation for little gain until the Autopilot service exists. |
| 137 | Diets, allergens, proteins, and equipment are closed lists that the heuristics understand. Cuisines, tags, and excluded ingredients are free text, normalized to lowercase and bounded. The vocabulary endpoint offers catalog-derived cuisines, tags, and protein counts plus starter lists (at most 60) | Hard constraints and rules need values the code can evaluate; cuisines and tags come from the catalog and vary. Starter lists make onboarding work before any recipes exist. |
| 138 | Reading Autopilot data needs `household.view`; changing the profile, week contexts, overrides, and generating, swapping, accepting, or rejecting need `plan.edit` | Accepting changes the plan, and preferences decide future plans, so it's the planner's permission. Members can already plan, so they can use Autopilot too. |
| 139 | iOS specialty ingredient setup is a sheet from the Pantry tab's More menu ("Specialty Ingredients…"). A grocery list opens the same sheet from a "needs a choice" notice, and each unchosen line's **Choose** opens a quick picker sheet (suggested options, Keep as Is, and See All Options, which pushes the detail inside the sheet) | Choices and batches need `pantry.edit` (#121) and batches live in the pantry (#117), so setup sits beside Low-Stock Alerts (#107). The tab bar is full (#83), and a sheet works the same from the Pantry and Week stacks without switching tabs. The picker keeps a shopper on the list, like "Add to pantry?" (#106). |
| 140 | The grocery list shows a raw ingredient under its "Make This Week" batch only when every `via` is that one house-made batch and every recipe on the item needs that batch. An ingredient that's shared (a recipe uses it directly, or another batch or a store alternative needs it) stays in its aisle with its `via` text. Batches already in the pantry are a compact "Already Made" section | Grouping a shared line would hide that part of its amount is for something else, and splitting an exact aggregated amount on the device would contradict the grocery engine (#120). Lines bought only for a batch, grouped with it, read as the shopping list for that jar. |
| 141 | Choosing an option or tapping **Made It** on a grocery list closes the picker or confirmation at once, saves in the background with progress on the row, then reloads the list. A failure is an alert with Try Again. A batch keeps its `clientPurchaseId` until it's recorded. House-made lines never ask "Add to pantry?" | The same non-blocking flow as purchases (#106). One ID per batch makes a retry after a lost response record one batch (#96, #117). A house-made line is already in the pantry, and a purchase would restart its cycle. |
| 142 | `SpecialtyStore` counts successful changes in `revision`, and an open grocery list reloads when it changes, skipping a revision the list caused itself. A recorded batch hands its pantry item to `PantryStore` through a closure set in `AppDependencies` | Choices change grocery lists and batches change the pantry, but the stores shouldn't depend on each other (like #112 in the API). A counter is cheap to observe and doesn't reload lists on unrelated pantry or plan changes. |
| 143 | **Customize** edits a copy in a form, saves it as a household option with `basedOnOptionId`, then chooses it. If choosing fails, the saved option's ID is kept, so Save chooses it instead of adding another copy. Only household options offer Edit and Delete | Curated options are read-only (#115). Two requests can half-succeed; remembering the created ID keeps retries from filling the 10-option limit with duplicates. |
| 144 | Every specialty change (Use Suggested for All, Choose, Keep as Is, Clear Choice, Customize, Edit, Delete, Made a Batch, Made It) is hidden without `pantry.edit`; those members see the same screens read-only. A `403` shows the error, turns list actions off, and reloads the household's role | Mirrors #121 and the pantry's convenience-only hiding (#84, #107); the API still checks every request. |
| 160 | Shopping is two packages: `internal/providers` is pure (the `GroceryProvider` interface, Walmart link parsing and cart links, package math) and `internal/shopping` owns household state (settings, saved products, handoffs, confirmations). Search, lookup, stores, cart write, and order import are optional interfaces found by type assertion; `GET /shopping/providers` reports them | Provider rules are table tested without a database or HTTP client, and 8b's signed Walmart client or 8c's Instacart client adds a type without changing the household module. Optional interfaces avoid stub methods that only return "unsupported". |
| 161 | A new `shopping.edit` permission (admins and members) guards store settings, saved products, and creating handoffs. Reading them and the match preview need `household.view`. Confirming an order needs `pantry.edit`, because it writes pantry purchases | The plan names it, and it keeps a future `shopper` or `viewer` role expressible: choosing products and handing off a cart is separate from changing the pantry. Reusing `pantry.edit` for handoffs would tie the two together. |
| 162 | A saved product comes from a pasted Walmart link or item ID. The item ID is read from the text only: `https`/`http`, host `walmart.com` or `www.walmart.com`, no user info or port, path `/ip/<id>` or `/ip/<slug>/<id>`, 5–15 digits. Short links (`walmrt.us`) and other shapes are rejected; the page is never fetched. The stored and returned product URL is rebuilt from the ID. The member types the name and an optional package size | Phase 8a has no Walmart keys and Walmart's terms forbid scraping. A strict allowlist can't be pointed at another host, and rebuilding the URL means pasted slugs and tracking parameters are never stored or shown. |
| 163 | Package counts are exact: amounts that convert to the package size's unit are summed and divided, rounded up. Discrete units convert only to themselves. Amounts that don't convert are left out and flag "check amount" (`unit_not_convertible`); nothing convertible, or no package size (`no_package_size`), is 1 package, flagged. More than 99 is 99, flagged. Flags never block a handoff | Same rule as #98 and #116: nothing is guessed. A count of 1 keeps the product in the cart, where the member can change it before checkout, and the flag makes the guess visible. |
| 164 | Cart links use the consolidated `https://www.walmart.com/sc/cart/addToCart?items=ID_QTY,ID&storeId=N` format (quantity suffix only above 1), merge lines that share a product, keep aisle order, and start a new link past 2,000 characters (after any affiliate wrapper) or 40 products. Every link carries the store | Walmart documents no length or item limit, so both are conservative and live in one function with tests. Opening links in order fills one cart. A failure Walmart reports for one product then affects a smaller cart. The 8.0 spike should confirm or raise them. |
| 165 | A handoff's candidates are the week's `toBuy` lines, unless the request selects lines (then exactly those, any status). Checked-off keys the app sends, excluded keys, and house-made batches are never included. Lines without a saved product are excluded as `no_product` and listed. `POST .../match` previews without storing; `POST .../handoffs` stores a snapshot of lines, exclusions, and links | The server doesn't store check-off state, so the app sends it. A snapshot means what a member confirms matches the link they opened, even if the plan or pantry changes afterwards. |
| 166 | Confirming writes one pantry purchase per line from the stored line: `unit: package` with `unitSize` for a measured size, the exact amount for a discrete size (2 × 12 count = 24 count), or a bare package count. It's idempotent per line through a unique partial index on `pantry_purchases {householdId, provider.handoffId, provider.lineId}`, plus a one-minute claim on the handoff line so concurrent confirmations don't restock twice. A stale claim can be taken over, and the unique index still returns the first purchase | Apps can't send `source: provider` (#97), and the confirm call names lines, not amounts. The claim prevents double restocks, and the index keeps retries after a crash safe. Skipped lines can still be confirmed later, because members often remember afterwards. |
| 167 | Handoffs and confirmations record server-observed `shopping.handoff_created` and `shopping.order_confirmed` events with counts only, never product IDs | They're behavior Autopilot can use (how often the household shops through a provider). Walmart forbids passing its product data to third parties, so product identifiers stay out of events. |
| 168 | The Impact affiliate wrapper is on only when `WALMART_IMPACT_PUBLISHER_ID`, `WALMART_IMPACT_AD_ID`, and `WALMART_IMPACT_CAMPAIGN_ID` are all set (a partial set fails startup). Responses carry `affiliateTracked`, which the app uses to show the disclosure | Nothing is needed to ship 8a, and it's unverified whether `goto.walmart.com` still opens the Walmart app. The flag keeps the FTC disclosure tied to whether links are actually tracked. |
| 170 | iOS keeps Autopilot in one main-actor `AutopilotStore`: the profile and vocabulary, and the shown week's context and proposal. The proposal never becomes `PlanStore` entries; accepting hands the returned `plan` to `PlanStore.present` through a `planDidChange` closure wired in `AppDependencies` | Mirrors the API's separation of proposals and plans (#124), so suggestions never reach the grocery list or other members' week, and the accepted plan shows without another request. |
| 171 | Onboarding is offered automatically once per user and household on a device (`AutopilotPromptStorage` in `UserDefaults`), on the Week tab's first visit while `configured` is false, and only to members with `plan.edit`. It stays reachable from **Set Up Autopilot** (**Run Setup Again** once configured, starting from the current answers) in the Week menu and Preferences, and **Plan with Autopilot** opens it while the profile isn't configured | The first visit is when planning is on the member's mind, and a prompt that returns on every visit nags. Members without `plan.edit` can't save a profile (#138). |
| 172 | Onboarding edits a local copy of the loaded profile, one step per section. **Skip** restores that section from the loaded profile, which is the API's defaults before setup. **Finish** validates locally, then sends one `PUT` with all seven replaceable sections and explicit `null`s, and offers **Plan My Week**, which generates the current week and opens the review. Superseded in part by #330: setup now asks three of those sections, and the `PUT` carries the loaded defaults for the rest | One attributed change instead of seven, and a skipped step keeps sensible defaults. Explicit nulls keep "household default servings" and "no limit" unambiguous. |
| 173 | Preference screens save one whole section with `PATCH`. Removing equipment strips that method from weekday rules on the device and drops a rule left with no preference, and saving equipment then also sends `weekdayRules` when they changed | The API validates the whole profile, so removing a smoker used by a Sunday rule would otherwise fail with `400`. |
| 174 | Vocabulary `limits` are enforced as the member edits: list sizes, value, ingredient, and label lengths, rule values, and number ranges (steppers). Liking removes a value from dislikes and exclusions, and excluding removes it from likes. Custom values are normalized like the API's free text. A `400 validation_failed` message (for example, two spellings of one cuisine) is shown as-is | Saving rarely fails, and the API's messages are documented as safe to show for what only it knows, such as canonical cuisines. |
| 175 | Suggestions are reviewed in a sheet (`ProposalReviewView`) opened from a Week list row, with each slot's day, photo, `cookMinutes`, a quick/medium/long badge, and reasons joined with " · ". Switched-off slots are local state for that proposal ID (kept across swaps, reset when it's generated again) and sent as `excludeSlotIds`. Every swap, accept, and dismiss sends `version`; `proposal_changed`, `proposal_not_pending`, or `404` reloads the proposal and shows a short notice instead of an error; `no_alternative` shows the API's message and keeps the meal; `plan_finalized` closes the review and offers **Reopen and Plan** through `PlanStore.setStatus(.draft)` | Keeps suggestions visibly apart from the plan (#124) while two members can act on one proposal (#125). A stale version is expected, not a failure. |
| 176 | "This Week's Plans" edits the whole week context in a sheet and saves it with one `PUT` (every field, explicit `null`s, empty day overrides dropped) or clears it with `DELETE`. Afterward an alert offers **Regenerate** (or **Plan with Autopilot**). The Week list shows a one-line summary such as "Busy weeknights · Up to 20 min" | The API replaces contexts whole (#132). Offering to plan again right away connects the change to its effect. Busy weeks only affect weeknights (#193), so the copy says so. |
| 177 | The Week tab loads the shown week's proposal and context together whenever the household or week changes (a proposal `404` means none) and on pull to refresh; nothing is cached across weeks | Two small reads per week switch keep other members' proposals and contexts current without invalidation. |
| 178 | Recipe rows and details show `cookMinutes`, falling back to the larger of prep and total time for a server without it. Plan entries decode `origin`, and Autopilot entries show a small sparkles badge; cooked and skipped events still carry `entryId` | Imported total times are unreliable (#127). The badge makes accepted meals recognizable without treating them differently, and `entryId` is how conversion joins back to proposals (#135). |
| 179 | A recipe's screen shows its Autopilot cook time and band and a **Good for Smoker** (grill, air fryer…) menu: Automatic (naming the heuristic's answer), Yes, or No, written with `PUT .../override` (`null` for automatic). Only the household's equipment is shown once the profile is configured; every method before that. Attributes load with the screen and aren't cached | Overrides fix the method heuristic where the household knows better (#130), and methods the household can't cook add noise. |
| 180 | Autopilot controls are hidden without `plan.edit` (onboarding, planning, swap, accept, context and preference saves, method overrides); preferences and suggestions stay readable, and a `403` shows the API's role message. `AutopilotStore` resets on sign-out and clears itself whenever a different household is activated | Matches the API's permissions (#138) and the other stores' reset behavior. |
| 181 | Cuisine chips keep the vocabulary's order (most used first) and labels, without grouping regions apart from specific cuisines; `cuisineRegions` on recipe attributes decodes as optional | The vocabulary doesn't say which values are regions (#191), and guessing from names would drift from the API's table. Older servers don't send `cuisineRegions`. |
| 190 | Autopilot reads every cuisine through one canonical form (`recommendations/canonical.go`): a small table of values, aliases, labels, and parents, plus a rule that turns a trailing region noun into its adjective (`southern europe` → `southern european`). Vocabulary counts, recipe attributes, likes, dislikes, exclusions, and weekday rules all use it; stored profiles are normalized on read and on write. Tags only merge spellings missing a space | An imported catalog labeled the same food "North America" and "North American", which split chips, counts, and likes. Canonicalizing on read keeps existing profiles working without a migration. Tags differ by marketing label rather than synonym, so a table would only guess. |
| 191 | Cuisines form a hierarchy instead of collapsing: `italian` → `southern european` → `european`, and `east asian` → `asian`. `american` is an alias of `north american`; `mediterranean` and `fusion` have no parent | Liking a region should cover its countries without losing the specific choice. The catalog uses "Asian" for East and Southeast Asian dishes alike, so it's a parent, not a synonym of "East Asian". |
| 192 | The provider's `Item` carries `CuisineRegions` apart from `Cuisines`. Likes, dislikes, exclusions, and rules match cuisines or regions; variety compares only a recipe's own cuisines. Vocabulary counts roll up to regions, and the attributes response adds `cuisineRegions` | Region-level variety made Italian and French, or Indian and Japanese, count as repeats and overrode other signals such as regenerate's avoidance. Rolled-up counts say how many recipes a chip matches. |
| 193 | A busy week is about weeknights: `busy` applies the quick soft limit, the zero long-meal allowance, and "at least half quick" only to the profile's `weeknights`. Other days keep their usual handling, days whose rule has `timeBand: long` keep their long cook even on weeknights, and `maxMinutes` (week or day) stays a hard cap everywhere. `busyWeek` reasons appear only on weeknights; other days under the week cap say `dayLimit` | A busy week made a Sunday smoker rule pick a 20-minute pork chop. Households mean busy evenings after work; a weekend long cook they planned for stays unless they cap that day. |
| 200 | The Shop tab's state is one app-lifetime `ShoppingStore` (main actor): setup, the week's match, changed package counts keyed by `ingredientKey`, cart-link progress, the week's open handoff, and saved products. The match is requested again when a week loads, after a product is saved or removed, and after an order is confirmed, and is never cached | The API doesn't store matches, and line IDs change between matches, so counts are keyed by ingredient. One store lets the tab shell ask "Did you order these?" over any tab and reset everything on sign-out or a household switch. |
| 201 | "Needs a Product" comes from `excluded` lines with `no_product`, "Ready" from `lines`, and "Not Included" from the other exclusions. A handoff sends only `checkedOffKeys` until a count changes, then every ready line as `lines`, with `packages` only on changed lines. A changed count's coverage is written on device | `lines` replaces the default candidates, so sending only changed lines would drop the rest, and omitting unchanged counts keeps the API's computed count. Matching again with `lines` would turn no-product lines into `not_selected` and empty "Needs a Product". |
| 202 | The grocery list and the Shop tab share one `GroceryCheckStorage`. The match sends its keys as `checkedOffKeys`; confirming an order adds the confirmed lines' keys; a grocery list reads checks again when it appears | The server doesn't store check-offs (#165). A confirmed line that's checked off never asks "Add to pantry?", which would record a second purchase. |
| 203 | Cart links open with `UIApplication.open` (the Walmart app through its universal link, otherwise Safari), one per tap: "Open in Walmart" creates the handoff and opens the first link, then "Next Cart Link (2 of 3)" opens each of the rest. The handoff ID stays with the progress | Opening several links at once loses all but one when the app leaves the foreground. A tap per link fills the same cart in order (#164). |
| 204 | The tab shell asks "Did you order these?" when the app becomes active and the shown week has an open handoff, unless its links aren't all opened, the member answered "Not yet" (until its links are opened again or the app relaunches), or the role lacks `pantry.edit`. The Shop tab also shows a banner for it. "Ordered Everything" sends `{all: true}` unless a count changed or a line was skipped earlier; the checked subset sends `lines` with `skipRest: true`. A `409` is retried once | Walmart reports nothing, so returning from it is when the member remembers. Confirming is idempotent per line (#166), so the retry can't record twice. `all` leaves earlier skipped lines out, so those are named. |
| 205 | Choose Product reads pasted text through the system `PasteButton`, keeps the first `http(s)` link `NSDataDetector` finds, or sends a bare 5–15 digit item number as `productId`. "Search on Walmart" is a plain `Link` to `walmart.com/search?q=`. The app fetches neither, and a `400` message shows in the form | Walmart's share sheet copies a sentence around the link, which the API rejects (#162). `PasteButton` reads the clipboard only when tapped, without the "Allow Paste" alert that `UIPasteboard` raises. Walmart forbids scraping, and product lookup waits for 8b. |
| 206 | `APIRequest` can carry an already percent-encoded path. Saved-product paths encode the `ingredientKey` segment strictly (RFC 3986 unreserved characters and `:`) | `URL.appending(path:)` encodes `%` again (`name:red%2520onion`) and treats `/` as a separator, and a `name:` key can hold spaces, `&`, or `/`. |
| 207 | Store setup, product changes, and handoffs are hidden without `shopping.edit`, and confirming without `pantry.edit`. A `403` reloads the household so the controls match the role. `HouseholdPermission` mirrors `shopping.edit` (#161) | The API enforces both; hiding is a convenience, like the grocery list's purchase card (#106). |
| 230 | Meal categories are a closed list of nine (`pasta`, `soup`, `salad`, `tacos`, `curry`, `bowl`, `stir-fry`, `sandwich`, `pizza`), derived at read time from the name, then tags, then cuisine, with the **last** keyword in the name winning and at most one category per recipe. Households override them per recipe like cooking methods | Pairings need a "kind of dish" recipes don't carry. English dish names end with the dish, so "Spicy Beef Taco Rigatoni" is pasta and "Chicken Noodle Soup" is soup; a closed list keeps rules, the vocabulary, and explanations small, and overrides fix what keywords miss. |
| 231 | A learned pairing must beat the add-on's rate in the weeks **without** the category by 0.2, on top of ≥3 weeks together and ≥0.5 confidence, with ≥4 weeks to compare against | Week-level confidence alone is inflated: imported meal-kit add-ons collapse many weekly menu aliases into one recipe, and the owner's "Garlic Bread" carries 203 of 230 order weeks (88%), giving 0.88–1.00 confidence for every category and a lift under 1.13. Comparing against the weeks without the category leaves exactly one real pairing, pasta → garlic bread (0.95 vs 0.49). |
| 232 | Pairing rules are a profile section (`pairings`) with server-assigned stable ids, but `PUT /profile` keeps them when it doesn't send them, unlike every other section | Rules are made from suggestions, not in onboarding, and the shipped iOS onboarding sends every section it knows; resetting them to empty would silently delete household habits. Ids let the app and "make it a rule" address a rule without matching on its contents. |
| 233 | Add-on entries are ordinary plan entries with `origin: autopilot` on the meal's day, and the adapter and accept skip them when deciding which days are taken | A pairing is not a dinner. Without this, garlic bread on Monday would block Monday and count toward the week's meals, so Autopilot would plan one dinner too few. |
| 234 | Paired grocery items live in `autopilot_week_pairings` and reach the grocery list through a new optional `planning.ExtrasSource`, as a line keyed `name:<normalized name>` with provenance in a new `extras[]` field | There is no manual extras concept to reuse, and planning shouldn't depend on Autopilot. The hook mirrors the pantry and specialty hooks, gives the Walmart handoff the item for free, and `extras[]` avoids overloading `via[]`, whose shape and enum the iOS client already decodes for specialty ingredients. |
| 235 | An add-on already planned is excluded per **day**, a grocery item per **week** | Two pasta nights each deserve their own garlic bread, but one box of crackers covers the week. A recipe that already lists the item as an ingredient also counts as covered. |
| 236 | Proposal slots store a pairing snapshot, and accept takes `pairingIds` (absent = the `always` ones) | The review screen renders and accepts without recomputing suggestions, the choice is explicit rather than positional, and an older client that sends no field still gets the household's `always` rules. |
| 237 | `GET /recipes/{recipeId}/pairings` is a separate read that includes pairings already in the week (`inPlan`) instead of hiding them | The detail screen's "Tastes even better with" carousel shows a stable row of cards with checkmarks, before the recipe is planned; the week endpoint hides what's already there because it drives "add this?" prompts. |
| 210 | On a weekday rule with methods, suiting a method dominates the `rule` signal: a meal that suits none gets −1 on an `every_week` rule and 0 on an `at_most_once` rule, whatever its proteins, cuisines, tags, or time band match. It stays soft, and a day filled with such a meal gets a `rule_method_unmet` week message. Rule methods count only when they're in the household's equipment. Model version `baseline-2026.2` | Partial credit (protein and long cook, rule 0.5) let a familiar chicken pot pie win a chicken-or-pork smoker night over smoker-friendly pork. The method is what the household means, and protein and time only narrow it. A hard filter would leave the day empty in catalogs with nothing suitable. At −1 × 0.45 the gap to a suitable meal is 0.9, more than familiarity, recency, and a good rating together. |
| 211 | A weekday rule's label is a reason only when the meal is what the rule is about: it suits the rule's method, or fully matches a rule without methods. Other positive matches list only what matches ("Mexican"), and a meal that misses the method has no `rule` reason | "Smoker night · Chicken · Long cook OK" on a pot pie read as a claim that it's a smoker meal. |
| 212 | The smoker heuristic rejects dish shapes named in the name's main part (before "with", "over", or "in"): pasta and noodles, soups and stews, pot pies, casseroles, stir-fries, tacos, bowls, sandwiches, burgers, meatballs, sausage, and similar. Cuts that are chopped, strips, cutlets, pulled, sausage mix, or patties don't count, and pork filet does. An explicit smoker tag or utensil still wins, and a dish shape outranks "smoked" in the name. Fish isn't included | Checked against an imported catalog of about 400 main meals: pork filet (the same cut as tenderloin) was missed, and a pasta with sliced chops counted. Meal-kit chicken comes as cutlets, strips, or diced meat, so no chicken recipe qualifies, which matches "a whole or large cut". A dish shape describes the meal better than a smoked ingredient does. |
| 250 | The Menu screen is served by a new `internal/menu` package that composes recipes, ratings, planning, Autopilot, and events through small interfaces it declares itself. It owns no collection and no store | Recipes and Week merge in the app, not in the data. A composing package keeps the merge out of modules other work is changing, and the interfaces make it testable with made-up recipes. |
| 251 | `GET /menu`, `/menu/recipes`, and `/menu/filters` read the whole catalog once per request through `recipes.MenuCatalog` (a projection with ingredient names but no amounts, steps, or descriptions) and compute sections, filters, and sorting in memory. Nothing about a menu is stored or cached | A household has a few hundred recipes, so one projected read is cheaper than a query per section, and every response reflects the current ratings, orders, and preferences without invalidation. Measured on an imported catalog of 431 recipes: `/menu` about 95 ms, of which the catalog read is 13 ms. |
| 252 | Sections are returned in a fixed order, an empty section is omitted, each holds at most 12 items, and ordering is total (signal, then name case-insensitively, then ID). Add-ons appear only in `sides` and the history sections; `history_planned` includes them because it is a record of the plan | The app renders whatever comes back, in order, so the server decides relevance. A total order makes paging and tests deterministic, and a side dish among dinner ideas reads as a mistake — except where the section is history. |
| 253 | The favorite score is `(average rating − 3) + 1.5 make-again + 0.75 kid-favorite + 0.8·log2(1 + times ordered) + recency (1 → 0 over 52 weeks) + 0.5·log2(1 + times cooked)`, minus 5 for never-again. A recipe is a favorite with an average of 4+, a make-again or kid-favorite tag, 3+ orders within two years, or 2+ cooks | Explicit opinions outweigh habit, but a household that reorders something 20 times has said something too. The logarithm keeps one very frequent meal from filling the row, and the qualification keeps "Your Favorites" to meals the household would recognize as favorites. |
| 254 | `sort=recommended` is a deterministic score (favorite score + taste fit, lowered by 3 for a recipe that breaks a hard restriction) rather than the Autopilot provider's `RankMeals` | `RankMeals` ranks one day of a week against fixed meals and week-level balance; browsing a catalog has no day and no week. Scoring in the snapshot the sections already build costs nothing extra, is stable between requests, and explains itself in the same words as the cards. Restricted recipes are lowered, not hidden, because All Meals is the whole catalog. |
| 255 | A `/menu/recipes` cursor encodes the last item's sort key (score, orders, week, minutes, lowercased name, ID), not an offset, and is rejected when sent with a different `sort` | Scores move as the household rates and orders, and an offset would then repeat or skip recipes. A position stays correct across those changes, which is what "stable paging for the same inputs" needs. |
| 256 | Card badges come from a closed set of eight codes, at most two per card, in one priority order; a section suppresses the badge that repeats its title and a smoker rule's section promotes `smoker_friendly` | A closed set lets the app give each badge an icon without parsing text. Two keeps the card readable, and "Quick" on every card in Quick & Easy is noise. |
| 257 | Hard restrictions (diets, allergens, excluded cuisines, proteins, tags, ingredients, no spicy) and never-again ratings filter the suggestion sections — `quick`, `new_to_you`, `rule_<day>`, `long_cooks` — but not `favorites`, the history sections, or All Meals | Suggesting a meal the household excluded is the one mistake a menu can't make. History is a record of what happened, and All Meals is the catalog: hiding rows there would look like missing data. |
| 258 | Weekday rule sections mirror Autopilot's rule scoring (#210): a rule's methods dominate, so a meal must suit one to appear; otherwise it needs at least half the rule's groups. Rule methods count only when the household has that equipment, and a rule with only a time band lists that band | A "Smoker Night Ideas" row that suggests a pot pie is worse than a short row. Reading the same attributes and the same method rule keeps the row and the Autopilot proposal from disagreeing about what smoker night means. |
| 259 | `RecipeSummary` everywhere gains `calories`, `proteinGrams`, and `timeBand`, and the recipe detail gains them too. Nutrition names and units are matched case-insensitively ("Calories", "Energy (kcal)", "Protein"), and energy reported only in kJ is converted | Cards show "30 min · 690 cal · 36g protein", and a card is a summary wherever it appears. The imported catalog spells energy three ways and 5 of 431 recipes carry only kJ, so parsing by exact name would show a blank. |
| 260 | `GET /weeks` counts planned entries from plan summaries, cooked meals from `recipe.cooked` events (distinct by entry, else by recipe and date), and ordered meals from recipe `orderWeeks` with add-ons excluded. `earliestWeek` is the earlier of the first planned week and the first ordered week, and plan reads are chunked to planning's 26-week range | The three counts come from three sources that already exist, so the strip needs no new collection. `earliestWeek` tells the app how far back to let the strip scroll, and chunking keeps a 105-week strip within the range planning accepts. |
| 261 | The recipe detail's ingredient lines carry the catalog ingredient's `imageUrl`, filled in beside the category on read. There is no per-ingredient allergen data: the import format carries allergens only per recipe | The catalog already stores the image the import supplied, so the detail screen needs no second request. Deriving per-ingredient allergens from the recipe's list would be a guess presented as data. |
| 270 | The Recipes and Week tabs become one **Menu** tab, so the tabs are Menu, Shop, Pantry, Household. The day-by-day week stays as `WeekView`, behind a **By Day** segmented control in the Menu screen's toolbar; both halves share one `WeekAutopilotFlow`, and `PlanStore` owns the shown week, which the strip changes and the menu follows | Recipes and the week in one page was the point of the redesign, and a toggle keeps every planning action (days, cooked and skipped, finalize, the entry editor, Add Recipes) reachable without rewriting it. One week in `PlanStore` keeps the menu, By Day, the grocery list, Autopilot, and the recipe screen on the same week. |
| 271 | `MenuStore` holds the menu; the plan stays in `PlanStore`, which hands every plan the server returns to `planDidChange` → `MenuStore.applyPlan`. That recomputes each card's `inPlan` and `planEntryIds` from the plan's entries and updates the week's planned count. Full menu reloads happen on pull to refresh, week switches, and after Autopilot's picks are accepted | The plan has one owner. Patching from the plan is exact and immediate, so a card's ✓ and the bottom bar don't wait for a round trip, while the reloads pick up the badges and reasons only the server can compute. |
| 272 | Sections render generically in the server's order: an unknown `kind` falls back to a carousel, an unknown badge code shows its text without a symbol, and "Show More" passes the section's `moreQuery` straight to All Meals | The API owns which sections exist (`favorites`, `rule_<day>`, `history_*`…). Hard-coding them would need an app release for every new one. |
| 273 | The week strip synthesizes its weeks from `oldestWeek` through four weeks ahead and decorates them with the server's summaries. Scrolling back asks for eight more weeks and stops at `earliestWeek`; when `/weeks` fails, a year of weeks still shows, without counts, and **Past** opens the same weeks as a list | The strip is navigation first: a failed counts request shouldn't make weeks unreachable. |
| 274 | **+** on a card adds the recipe to the shown week at once — unscheduled, with `preferredServings(householdDefault:)` (#66) — and shows an undo toast; long-pressing **+** offers the days. **✓** removes the most recently added entry for that recipe, also with undo. Past weeks and finalized weeks show no **+** | Planning a week means adding several meals in a row, so the common case is one tap; a sheet per add is friction, and undo makes both directions safe without a confirmation. |
| 275 | The recipe screen's bottom bar is **Add to Week** until the recipe is in the shown week, then the stepper "− 1 in your week (2 servings) +": **+** and **−** move to the recipe's next authored serving size, and **−** at the smallest size removes the meal with undo. Planned more than once, the count reads "2 in your week" and the stepper acts on the most recently added entry, with a day menu beside it for moving or removing it. Your Meals cards carry the same compact stepper | Ingredient amounts are authored per serving size and are never scaled, so stepping between authored sizes is the only correct change. Acting on the newest entry matches the one a second add just created, while the count keeps the others visible. |
| 276 | The stepper and the selected week pill use the herb-green `AccentColor`, not the reference app's black | DinnerOS's own identity (no HelloFresh cloning), and a black bar would disappear in dark mode while the accent reads as an action in both. |
| 277 | The recipe screen keeps the system navigation bar — transparent over a stretchy hero about 45% of the screen tall, with a top scrim — and shows its title and background once scrolled past. Share is a toolbar `ShareLink` carrying the recipe's `sourceUrl`; on iOS 18 it gets a circular material background, on iOS 26 the system's own | Hiding the bar for custom circular buttons loses the swipe-back gesture and the system's Dynamic Type and accessibility handling. The scrim keeps the back and share buttons legible over bright photos. |
| 278 | The recipe screen's sticky picker is Overview / Ingredients / Nutrition. Customize is the first section inside Overview, then pairings, then the description, Autopilot, ratings, and order history; Cooking Steps is a disclosure section under the picker's content | Keeps the reference layout's order ("customize first, then what goes with it") without a fourth tab for a section most recipes don't have, and leaves the steps reachable from every tab. |
| 279 | Customizations and pairings are loaded per recipe, never cached, and a `404` from either endpoint means "none", which hides the section. A choice saves to the planned entry at once (optimistically, put back on failure); with nothing planned it's kept and sent right after **Add to Week** succeeds | Both APIs are being built in parallel, so the screen has to work before and after they merge. Saving on selection matches a radio control's feel; keeping it locally avoids forcing an add before a choice. |
| 280 | Photos ask the CDN for the size they're shown at by swapping the imported URL's `w_<pixels>` segment (`RecipeImageURL`), rounded up to a few buckets and never above the original width; URLs without that segment are left alone. `URLCache.shared` is raised to 48 MB in memory and 256 MB on disk | A 1200-pixel photo per card costs several megabytes of decoded memory in a long list. Buckets keep one cached response per size class, and leaving other hosts alone keeps non-CDN images working. |
| 281 | Additive API fields are read through `decodeLenient` and `decodeLossyArray`: `calories`, `proteinGrams`, `timeBand`, ingredient images and allergens, `nutrition`, `sourceUrl`, and plan entry `customizations`. Unreadable elements of a list are skipped rather than failing the response | The menu API is merging in parallel. A field that arrives late, as a string, or not at all should cost one badge or one stat, not the whole screen. |
| 290 | Per-meal protein customization lives in a new `internal/customize`: a curated protein table, pure transforms, and the HTTP handlers. Planning, grocery, and pantry keep only small hooks — planning stores `Entry.Customizations` and calls a `CustomizationSource` before specialties, grocery gains a `customized` via kind, pantry gains an optional `CookAdjuster` | The feature touches three modules that other work is changing at the same time. Keeping the logic in one new package makes the hooks reviewable in isolation and the merges small, the way the specialty module did it. |
| 291 | Only ingredient lines in the curated table, with an exact weight for the serving size, get a customization group. Counts ("2 Pork Chops"), amount-less lines, and processed or pre-cooked meat (pulled pork, cooked meatballs, bratwurst, bacon) don't | A swap is a like-for-like weight: 10 oz of pork becomes 10 oz of beef. Nothing in the data says how much a "chop" weighs, so a count swap would be a guess, and the engine never guesses. Checked against an imported catalog of 399 main meals: 340 (85%) get a group, and only four weight-based protein lines are left out, all pre-cooked or processed. |
| 292 | Swap families are curated, not derived: ground meats swap with each other plus diced chicken; chicken cuts with each other plus pork chops and tofu; pork cuts and steaks with each other plus chicken; sausage with sausage and ground meat; seafood with seafood and chicken; tofu with chicken and shrimp. Pairs may override the same-weight default with a ratio | The reference app offers a short, sensible list per meal, not every protein. Deriving families from the protein heuristics would offer fish for a taco filling. A table is reviewable and cheap to extend, and the ratio hook is there for pairs that aren't one-for-one. |
| 293 | Choices are filtered by the household's Autopilot restrictions (excluded proteins and ingredients, allergens, and diets) at read time, and the same filter validates a `PUT`. The original choice is always kept, as is a choice the member already made | Restrictions are hard constraints, so offering a swap that breaks one would be wrong. But the original is what the recipe is, and hiding it would make the meal unreadable; silently dropping a stored choice would change a meal the member chose. A profile read failure fails the request rather than offering an unfiltered list. |
| 294 | `meal.customized` (`{entryId, changes: [{ingredientKey, from, to}]}`) is added to the closed payload set and recorded server-side, with `from`/`to` as choice IDs. V1 records but doesn't learn from it | It's a cheap, direct preference signal ("they always swap pork for chicken", "they always double"), and the payload holds the diff rather than the whole selection so the history reads as changes. Documented as a future learning signal in autopilot.md. |
| 295 | A cooked meal's entry is found by searching the household's plans from 8 weeks before the cooked date to one week after, instead of adding an entry lookup to the planning store | `recipe.cooked` carries an `entryId` but no week, and the store's index is per household and week. A range read reuses `ListPlans` and adds no store method for other agents to merge around; meals are cooked within days of their week, so the range is generous. An entry outside it deducts the recipe as written. |
| 310 | One `Pairing` model backs all three surfaces (the recipe carousel, the week's suggestions, and a proposal slot), and the server's `key` is stored and sent back verbatim — never rebuilt from the target | The three endpoints return the same `AutopilotPairing`, so one decoder keeps them honest. The key is the server's identity for accept, dismiss, and rules: the shipped carousel synthesized `item:<Name>` for grocery items, which would never match the API's `grocery:<lowercased name>` and would have failed as `404 not_found`. |
| 311 | Pairing rules are an `AutopilotSection` (`.pairings`) for attribution and `PATCH`, but the section list a full `PUT` sends is `AutopilotSection.replaceable`, which leaves them out | Rules are made from suggestions, not from onboarding, and the API **keeps** them when a `PUT` omits them. Treating pairings as an ordinary section would make the last onboarding step delete every rule the household had kept. |
| 312 | The recipe carousel plans the main recipe first when it isn't in the week (`entryId` is null), then accepts the pairing against the new entry; the meal is added without its own undo toast | A pairing always belongs to a planned meal, so there is nothing to accept against until the main recipe is planned. One tap on "add garlic bread" meaning "and plan the pasta" matches what the member asked for; two toasts for one tap would read as two separate changes. |
| 313 | The Menu's prompts are one "Goes well with your meals" section under Your Meals, listing the week's open suggestions with Add and "Not this time" — not an inline row on each meal card | Suggestions are about the week and several meals can have one, so a section stays quiet and collects them in one place. The cards already carry the servings stepper, day chip, and badges; a fourth control per card crowded them, and a section hides itself completely when a household has no pairings. |
| 314 | `PairingsStore` owns the shown week's pairings; every plan a pairing change returns goes through `PlanStore.present` and every new rule through `AutopilotStore.present`, the same way an accepted proposal does | The plan has one owner (#271). Accepting an add-on creates a plan entry and accepting a grocery item changes the week's list, so the Menu, the week, and the grocery list have to see both without a manual refresh. |
| 315 | The review sheet always sends `pairingIds` explicitly, never leaving the field out, and drops the pairings of slots the member excluded | The sheet shows a toggle per pairing, so the checked ones are exactly what to add; omitting the field would mean "the household's `always` rules" and would quietly re-add a pairing the member just unchecked. |
| 316 | Grocery `extras[].text` renders exactly like `via` — one quiet line under the item — and only extras with `origin: pairing` offer Remove, through `DELETE .../pairings/grocery-items/{id}` | Both answer the same question ("why is this on my list?"), so they should read the same. A paired item is the household's own add-on and can come back off; an ingredient a recipe needs can't, because removing the meal is what takes it off. An unknown origin is shown but not removable. |
| 330 | Autopilot setup asks three questions — taste, restrictions, schedule (`AutopilotOnboardingStep`) — and the cook-time mix, equipment, weekday rules, novelty, and pairings move to Preferences. The `PUT` still sends every section in `AutopilotSection.replaceable`, carrying the loaded profile's defaults for the five it no longer asks | Seven text-heavy steps read as a wall: the owner tuned out by the last few. Three picture-led screens get a household into the app in well under a minute, and nothing is lost because every section stays editable. Sending the whole profile keeps onboarding one attributed change (#172) and keeps a skipped or unasked section explicit rather than absent. |
| 331 | The cuisine grid's photos come from one small `menu/recipes?cuisine=…` list per tile (`CuisineTileLoader` through `MenuStore.makeList`, which now takes a `pageSize`), not from the menu's loaded cards | `RecipeSummary` carries no cuisine, so a loaded card can't be matched to one; the Menu's own paging endpoint already answers the question and needs no new API. Loads run sequentially and fill tiles as they arrive: the step is usable before the photos land. |
| 334 | The app keeps its own copy of the canonical cuisine hierarchy (`CuisineHierarchy`, mirroring `recommendations/canonical.go`) and offers the specific cuisines people recognize, falling back to a region only when nothing under it has `minimumSpecificRecipes` (5). A region is never offered beside a cuisine of its own | Vocabulary counts roll up, so ordering by count alone put every region above its own cuisines: the real catalog gave "Asian" next to "East Asian" and "European" next to "Southern European", which splits one preference across two tiles and reads as a bug. The vocabulary sends no parent, so the table has to live in the app; an unknown cuisine has no parent and counts as specific, which is the safe direction. |
| 335 | Tiles never repeat a photo: `CuisineTiles.assignPhotos` hands out candidates in tile order, each tile taking the first recipe no earlier tile used, and a tile with nothing left shows a plain tinted tile with its name rather than a duplicate | A recipe can belong to several cuisines, so the "top recipe" of two tiles was often the same one — three pairs of identical photos on the real catalog, which reads as broken. Assigning in tile order from a reassigned-from-scratch pass keeps the grid deterministic no matter which request finished first. |
| 336 | Setup opens on the first question instead of a welcome screen; **Skip** sits in the nav bar on every step and **Set Up Later** on the first step's bar | The welcome screen said what the first question already shows and cost a tap before anything happened. Leaving setup entirely still needs a home, and the first step is where someone decides whether to bother. |
| 332 | Every setup step has **Skip**, the welcome has **Set Up Later**, and a skipped step restores that section from the loaded profile rather than writing anything of its own | Setup must never be a wall between a new household and the app. Restoring from the loaded profile means "skipped" and "never asked" are the same thing on the wire, so the API's defaults stand either way. |
| 333 | Preferences lists sections in two groups, **From Setup** and **Fine-tune Autopilot** (`AutopilotSection.isInSetup`), with a one-line description under every row, and the Week menu's entry is renamed **Fine-tune Autopilot** | With setup no longer asking about five sections, the screen has to show where they went. Grouping answers "what didn't it ask me?" without hiding anything, and the descriptions replace the explanatory paragraphs onboarding used to carry. |
| 350 | Recipe photos go through one `ImageLoader` (`Core/Images`) instead of `AsyncImage`, holding decoded `UIImage`s in an `NSCache` keyed by URL **and** the pixel size they were decoded for | `AsyncImage` has no memory cache, so every card that scrolled out and back re-downloaded and re-decoded its photo — why a long Menu spent most of a scroll loading. Keying on size as well as URL stops a 160-point thumbnail from pinning a hero-sized bitmap, and lets both exist at once. |
| 351 | The key's size is a `RecipeImageURL` bucket, not the measured pixel width | A card measured at 370.33 points and one at 371 would otherwise be two entries holding two nearly identical bitmaps. Bucketing makes the key agree with the URL that was fetched: the same bucket always means the same download and one entry. |
| 352 | Photos are decoded with `CGImageSourceCreateThumbnailAtIndex` at the target size, never `UIImage(data:)` | A 1200×900 source costs about 4 MB resident whatever it is shown at; downsampled for a 220-point card it costs about 300 KB. Decoding once, at size, off the main actor is what removes the stutter — caching alone would only stop the refetching. |
| 353 | One fetch per URL: concurrent callers join the in-flight `Task`, and it is cancelled only when the last waiter leaves, by refcount rather than on the first cancellation | Several cards and the recipe hero ask for the same photo. Cancelling when the first view disappears would abort a download three on-screen cards were still waiting for, so the refcount is what makes coalescing and cancellation safe together. |
| 354 | Photos use their own `URLSession` with a 50 MB / 300 MB `URLCache` honoring HTTP caching headers, unlike the API session (`URLSessionTransport`), which caches nothing | Recipe photos are immutable and served with caching headers; API responses are neither. The disk half is what makes a relaunch cheap: the bytes are already local, so only the decode repeats. |
| 355 | The shimmer appears only after 100 ms, and a recipe with no image URL shows the fallback glyph immediately with no placeholder at all | A cached photo draws on its first frame, so showing a placeholder unconditionally made every scroll-back flash grey. Imported and hand-added recipes without a photo will never load anything, so a spinner there promises something that is not coming. |
| 356 | A failed photo is retried once after 300 ms, and only for a transient failure (a dropped connection, 429, or 5xx); a 404 settles on the glyph and is never retried | A blip is worth one more try while the card is still on screen. A photo that is genuinely gone would otherwise be refetched on every scroll-back forever — load with no prospect of an image. |
| 357 | Prefetching warms the next five photos and is attached to the card that appeared, not to the section | A section that is never scrolled to then warms nothing, and the window stays small on purpose: warming a whole 400-recipe list would compete with the photos actually on screen, which is the stutter this is meant to remove. |
| 358 | `RecipePhoto` measures its own frame and refines the width it asks for, falling back to the 640-pixel bucket until the layout has one | Callers sized by their container (the hero) or by the layout (a full-width card) would otherwise have to guess, and guessing high means downloading the 1200-pixel original for a card. Only a measured width that lands in a different bucket re-requests anything. |
| 359 | A card shows its photo and its name, and nothing else: the headline, the facts row (calories and protein), the reason line, and the badge row are gone from carousel, full-width, and Your Meals cards | What a meal is and how long it takes is what a card is scanned for; the rest is a dive into the recipe screen, which already shows Total Time, Calories, Protein, and Difficulty. Removing them also removes everything that varied per card, which is what made a row of cards different heights. |
| 360 | Every card in a row is the same height by construction: a fixed photo aspect ratio plus a name clamped and given a reserved height (`MenuCardMetrics.titleLines`), two lines normally and three at accessibility sizes | A long name or a missing subtitle used to change a card's height, which read as clunky in a carousel. Reserving the height rather than letting the row stretch to its tallest card keeps neighbours aligned, and scaling the reservation with the text size keeps them aligned under Dynamic Type. |
| 361 | The cook time is a badge over the photo's bottom-left, with `bolt.fill` and the accent colour only in the quick band; no time known, no badge | It is the one fact worth reading at a glance, and over the photo it costs no vertical space, so it can't make cards uneven. Reserving the bolt and the colour for quick recipes is what makes it mean something — a badge on every card would just be decoration. |
| 362 | At most one context badge, on the photo's top-left, in the order Autopilot pick, Make Again, Often Ordered (`MenuCardBadge.priority`) | Stacking badges is what the old badge row did, and it crowded the card and varied its height. One badge answers "why is this here?" and a fixed priority means the answer is the most useful one rather than whichever the API listed first. |
| 363 | The card's accessibility label carries what the badges say — name, time, "quick", the context badge, "add-on", and whether it's in the week — even though that text is no longer drawn | Stripping a card to a photo and a name is a visual decision, not a decision to tell VoiceOver less. The badges are marked `accessibilityHidden` and spoken once, in order, as part of the card's own label. |
| 364 | Everything that says "meals" counts main meals only (`MealCounts`), and names add-ons separately: "5 meals · 1 add-on" | A pairing's add-on is a plan entry, so counting entries made a week of five dinners and one garlic bread read "6 meals this week", which is simply wrong. Counting in one place means the bottom bar, Your Meals, and the week pills can't drift apart. |
| 365 | Whether an entry is an add-on is asked of `MenuStore.isAddOn`, which reads the entry's own `isAddon` and falls back to the menu's card for that recipe | `PlanEntry.recipe` doesn't carry the flag on an older server, and the counts have to be right before that ships. The entry is authoritative once it has the field; the card is what the app already knows in the meantime. |
| 366 | `WeekSummary.addOnCount` is decoded leniently beside `plannedCount`, and the optimistic patch `MenuStore` applies when a plan changes counts meals and add-ons apart rather than counting entries | The API's counts are authoritative (#374), but dropping the local patch would mean a week pill that doesn't move until a reload after adding a meal. Patching with the same meal/add-on split keeps the pill live without letting it disagree with the server. |
| 370 | Week variety penalizes a shared **meal category** (−0.20) and a shared **cuisine region** (−0.10) alongside the existing shared-cuisine (−0.20) and shared-protein (−0.15) penalties. Categories reach the optimizer as `Item.MealCategories`, derived by `recommendations` and passed in like cuisines; the cuisine and region penalties never stack | A "Monday: Italian, at most once" rule still produced a second pasta later in the week. Nothing in the objective looked at the *kind* of dish, so two pastas labeled `italian` and `southern european` — or one with no cuisine at all — read as unrelated meals. `internal/autopilot` stays standard-library-only and source-agnostic: categories arrive as opaque strings, exactly as cuisines do, and the keyword heuristic that derives them stays in `recommendations`. |
| 371 | A weekday rule's cuisines match **one step down the hierarchy**: a rule for `italian` matches a recipe labeled only `southern european`, but not one labeled `french`. The rule carries `CuisineRegions` (its cuisines' ancestors), and the match tests the item's *own* cuisines against them, never the item's regions | Catalogs label a lot of Italian food with the region alone, and such a recipe escaped both the rule and its `at_most_once` limit — half of the repeat the owner saw. The symmetric version would be far too loose: French and Italian are both `european`, and an Italian rule must not claim a French dinner. Likes, dislikes, and exclusions keep the stricter reading (#…), so liking `italian` still can't be more specific than the source label. |
| 372 | `Avoid` rises 0.30 → 0.35 and `ModelVersion` becomes `baseline-2026.3`. `RuleRepeat` stays 0.40 | The new variety penalties make a regenerated week's leftover meals score worse, and at 0.30 a regenerate repeated one meal from the proposal it replaced. 0.35 is the smallest step that restores a fully fresh week, and `Avoid` exists for exactly that. `RuleRepeat` needed no change: with rule, cuisine, and category repeats now stacking on a second full match, the regression test holds one Italian meal a week at 0.40, so the weight was never the problem — the missing signals were. |
| 449 | An `at_most_once` rule also limits the meal category of the meal that satisfied it; `ModelVersion` becomes `baseline-2026.4` | Rule matching reads cuisines, tags, proteins, and methods, so a pasta the catalog labeled `north american` never counted against a "Monday: Italian, at most once" rule — only the 0.20 category repeat pushed back, and a strong enough meal walked past it and served pasta two or three nights. The rule now remembers the categories of the meal it placed and charges `RuleRepeat` for a second meal of that kind. Categories stay opaque strings supplied by `recommendations`, so `internal/autopilot` learns nothing source-specific and no rule needs a new field: the household keeps answering the cuisine question it already answered. |
| 373 | Catalog tags are filtered before being offered as choices: a documented denylist of source bookkeeping (`seo`, `dinners`, `lto`, `static-position`…) plus any tag on more than 90% of the catalog. The filter lives in `buildVocabulary` | The Food Types list opened with "SEO 274" — the source's own bookkeeping, not a food characteristic — and a tag matching nearly everything filters nothing. `buildVocabulary` is the one place the menu chips, the Autopilot vocabulary, and the weekday-rule and taste pickers all read, so one rule covers every surface. Filtering before the 60-option cap means hiding a tag frees a slot. Canonicalization, validation, and matching are untouched, so a stored preference naming a hidden tag keeps working. |
| 440 | Specialty ingredients gain one **household strategy** (`similar`, `closest`, `ask`; `similar` by default) on a new `specialty_settings` collection, instead of only a decision per ingredient. It is resolved when a grocery list is built (`substitutes.resolve`), never written to the household's choices | Deciding 29 blends one at a time is a wall, and the owner's ask was a single standing answer: "something close from the store" or "I'll make a jar". Resolving at read time means changing the setting changes every future list with no migration and no stored choice to un-pick, the same read-time rule the specialty flag and pantry statuses already follow (#57). `similar` is the default because buying something close is the lower-effort path, and a household that wants the old behavior sets `ask`. |
| 441 | The strategy picks only **curated** options, preferring the specialty's `defaultOptionId` when it is of the wanted kind and otherwise the first of that kind, and falls back to the other kind when the specialty has only one | A household option exists because someone wrote it deliberately; choosing it for them would be guessing at intent, and they can simply select it. Preferring the curated default keeps the strategy's pick and the "suggested" option the same wherever both agree — for Ponzu Sauce, which has two store alternatives, `similar` lands on the default rather than an arbitrary one. The fallback is what makes the setting honest: `closest` still fills a stock concentrate that has no batch. |
| 442 | `choice` is now present whenever *anything* applies, with `choice.source` (`household`/`strategy`) and a sibling `choiceSource` (`household`/`strategy`/`none`). `chosenBy` and `chosenAt` became nullable, since a strategy pick has no chooser | The app has to answer "is this my decision or the default?" per ingredient, and a separate parallel object would have meant two shapes for one concept. Making the timestamps nullable is the one breaking edge: a client that requires them will see null on strategy-sourced rows, so the iOS contract notes call it out explicitly rather than leaving it to a decode failure. `choiceSource` is duplicated at the top level so a client can branch without a null check. |
| 443 | Every curated specialty ingredient must carry a store alternative **or** an explicit `note`, enforced by `TestSeedStoreAlternatives`. The audit found all 29 already had one, so the seed gained a `note` field (6 entries) and a better Tex-Mex paste rather than new options | The owner's worry was that `similar` would have nothing to buy for some entries. It turned out to be unfounded, and the valuable part was the honesty check, not new data — so the rule is now a test instead of a one-off review. The notes cover the real gap the audit did find: several store routes depend on an international-aisle product, which is a shopping caveat rather than a missing alternative. Tex-Mex paste gained chipotle in adobo, the owner's own suggestion, because the old version had no smoky element. |
| 444 | `specialty.strategy_updated` (`{strategy, previous}`) joins the closed payload set, recorded server-side on the setting change only | It is the same kind of household preference signal as `autopilot.preferences_updated` (#294): cheap, a diff rather than a snapshot, and a genuine taste indicator ("this household always makes the jar"). Recording it per resolved ingredient at list-build time would emit dozens of events for one decision, so only the decision is recorded. |
| 445 | A card never asks the CDN above the 1080-pixel bucket (`ImageKey.cardBucketCap`, the default for every `RecipePhoto`); the full-screen hero opts out with `maxBucket: nil` and keeps the 1200-pixel original | A full-width card is about 370 points, which at @3x rounds up to the 1200-pixel bucket — the card was downloading and decoding the same photo the hero gets. One bucket down is still more pixels than a card can show on any phone, and measurably cheaper: the benchmark's 40-photo All Meals scroll holds 118.6 MB of decoded photos instead of 146.5 MB. |
| 446 | The decoded-image cache's cost limit is a sixteenth of the device's physical memory, floored at 48 MB and capped at 192 MB (`ImageMemoryCache.costLimit`), rather than a flat 64 MB | The flat limit sat under the working set of a long scroll — about 119 MB of decoded photos even with #445 — so `NSCache` evicted photos that were about to be scrolled back to: the benchmark measured a 42.5% hit rate against the 50% ceiling that scroll pattern allows, and 50.0% once the limit was sized off the device. Sizing off memory keeps the cache proportionate on a small phone, the ceiling keeps it from becoming the reason a large one is killed, and a memory warning still empties it outright. |
| 447 | A card's name reserves 22 points a line, not 21, and is `fixedSize`d vertically inside that reservation | 21 was under what a line of `.headline` actually occupies, so a two-line name was offered less height than it needed and SwiftUI dropped the second line: every long name truncated to one ("One-Pan Santa Fe Pork T…") above an empty line. Reserving slightly more keeps #360's equal heights, and fixing the vertical dimension means `lineLimit` is what bounds the name rather than the box it sits in. |
| 374 | `GET /weeks` gains `addOnCount`, and `plannedCount` counts main meals only. `planning.Summary` carries the week's entry recipe IDs so the menu can split them against the catalog it already loads | The strip read "6 meals" for five dinners and a garlic bread. Planning has no idea what an add-on is — only the recipe catalog does — so the split belongs in `menu`, where `orderedCount` already excludes add-ons. Projecting the IDs keeps it within the single query the strip already makes, and a summary without them falls back to the entry count so nothing regresses. |
| 400 | "Request a store" is demand data in `internal/shopping`, with its own curated catalog in code (`catalog.go`), and `internal/providers` is left untouched | The provider registry answers "what can DinnerOS do", and a request answers "what do people want" — merging them would make an unbuilt integration look enabled, and every unsupported store would need a `GroceryProvider` that can't hand off. A committed Go slice beats seed data because the list is small, reviewed, and needs no migration to change. |
| 401 | The catalog's `status` mirrors [shopping-providers.md](shopping-providers.md#demand-signal) (`available` for Walmart, `researched` for the providers that document assessed, `unsupported` otherwise), and a banner inherits its parent chain's status | One source of truth for what we know: the field is research, not live capability, so it can't drift from the document that justifies it. Fry's and Ralphs are the same Kroger cart API under different signs, so giving them different statuses would misreport the same work. |
| 402 | A store request is unique per `{householdId, key}` and upserted: a second request updates the note and returns `200`, keeping the first requester and time. A typed name is matched against catalog names, keys, and aliases before becoming its own entry | A household wanting a store is one fact, not a counter they can inflate. Alias matching is what keeps the signal readable: without it "frys", "Fry's", and "FRYS" would be three separate rows and Fry's would look three times less wanted than it is. |
| 403 | `requests` and `requestedByHousehold` are computed per call — a `$group` over the collection and the household's own (capped) request list — rather than a stored counter | Both are cheap at this size and can't go stale or double-count the way a denormalized total would. The catalog route isn't household scoped, so `requestedByHousehold` resolves the caller's first household and is simply `false` for a user who belongs to none. |
| 404 | A household is capped at 25 stores (`409 conflict` past it, while note updates still work), and `shopping.store_requested` carries only the key and whether it was a catalog entry | The cap counts stores rather than requests, so the limit bounds the demand one account can manufacture without blocking someone editing what they already said. The event stays in the closed payload set and leaves the member's free-text note out: notes are for us to read on the record, not to forward into behavior history. |
| 420 | The store catalog and this household's requests live on `ShoppingStore`, not a new store | Sign-out, household switch, and the scope counter that stops a slow response writing into a new household already exist there, and the Shop tab is the only screen that shows them. A second store would duplicate all of it. |
| 421 | A `404` from `GET /api/v1/shopping/catalog` or `.../shopping/requests` sets `isCatalogAvailable = false` and hides "Don't see your store?", instead of showing an error | These endpoints are being built in parallel. A member shouldn't see "Something went wrong" for a feature that simply isn't deployed yet; every other failure still shows a message and a Try Again. |
| 422 | `kind` and `status` are `RawRepresentable` structs like the other shopping enums, and an unrecognized `status` shows no chip | Same rule as #422's neighbours in shopping: a value a newer API adds still lists and is still requestable. Only `available` and `researched` make a claim, so only they earn a chip. |
| 423 | A row whose status is `available` opens store setup instead of sending a request | Walmart already works; asking for it would record demand for something that's shipped and leave the member no closer to using it. The row keeps a disclosure chevron so it reads as navigation, not a request. |
| 424 | `requests` is shown only when it's greater than 1, as "3 households asked" | At 1 the only household that asked is usually the one reading it, which makes the count feel like surveillance rather than social proof. |
| 425 | The catalog is fetched whole once and filtered on the device; the API's `q` parameter is unused | The list is small, the empty state needs the most-requested and researched entries anyway, and filtering locally makes typing instant and offline-tolerant. `ShoppingAPI.catalog(query:)` still takes `q` for when the catalog outgrows this. |
| 426 | Both export surfaces build a `GroceryListModel` and call `GroceryListText`, rather than formatting the Shop tab's proposal | The proposal only holds lines with a saved Walmart product, so formatting it would export a different, shorter list than the Grocery List screen's Share button. Reusing the model means the Shop tab exports exactly what the grocery list shows. |
| 427 | Reminders is reached through the `GroceryRemindersStore` protocol; EventKit appears only in `EventKitRemindersStore` | Tests cover denied access, a new list, and an existing list without touching the member's real Reminders database, which a test against `EKEventStore` would write to. |
| 428 | One flat Reminders list per week, named "DinnerOS · Sep 14–20", with items added in aisle order and the aisle in each reminder's notes | Reminders has no sub-headings inside a list, so aisle order plus a note is the whole of "grouped by aisle". A list per week keeps last week's shopping from mixing into this week's. |
| 429 | An existing list with that name offers Replace or Add, and never picks for the member | Replace silently deleting reminders someone added by hand would be the worst outcome here, and Add silently doubling every line is the second worst. |
| 430 | Export is a plain `Form` section titled "Export the List" under the main flow, with no prominent button | The owner called it a last resort. Styling it like the Walmart handoff would compete with the flow the tab exists for. |
| 431 | Recipes with no `totalMinutes` are not backfilled. The importer flags only the recipes with neither time (`cookTime` review item); nothing derives a total from prep | The gap is the source's, not the importer's: HelloFresh omits `totalTime` for all 32 add-ons and 16 mains, and `import_reviews` has no duration failures. Cook time is already max(prep, total) at read time (#127), and every imported recipe has a positive `prepMinutes`, so a backfill would write numbers no one measured to fix nothing. |
| 448 | Requesting a store whose catalog `status` is `available` is a `400`, rejected in `newStoreRequest` on both the explicit `key` path and the typed `name` that `matchCatalogName` resolves. The catalog itself grows to the major US chains and delivery services, with banners inheriting a parent chain's `researched` status and everything unassessed staying `unsupported` with no note | The cap and the uniqueness rule bounded how much one household could distort demand, but nothing stopped it recording demand for work that is already done: Walmart ships, so a request for it is noise in the counts that decide what gets built next, and it leaves the member no closer to using the store they asked for. Checking the resolved entry rather than the raw input is what closes both doors at once — "walmart" typed as a name lands on the same entry as `key: walmart`, so a check on the key alone would have been trivially bypassed. Growing the catalog is the other half: a store missing from it is recorded as free text under its own key, so "Kroger's" and "Krogers" and "kroger" fragment into rows that each look unwanted. Statuses stay conservative on purpose — a chain we have not assessed is `unsupported`, never a guess at `available`, because #401 makes this field research rather than capability. |
| 450 | A plan entry's recipe snapshot carries `isAddon`, copied from the recipe when the entry is added (`Entry.RecipeIsAddon`, stored as `recipeIsAddon`) and always serialized by `EntryRecipeResponse`. The app trusts it: `MenuStore.summary(for:)` reads the week's split straight off the plan instead of keeping the server's counts when nothing on hand named the add-ons | An add-on is a plan entry like any other, so a client that isn't told has to work it out from the menu's cards — which load separately from, and often after, the plan. The guess that filled the gap was wrong in exactly the case it existed for: a week of seven dinners and one add-on read "8 meals" in the strip while the bottom bar and Your Meals said seven. A snapshot beside `recipeName` and `recipeImageUrl` follows the rule those already set (a plan renders without loading recipes) and costs one bool per entry; resolving it at read time would mean loading every week's recipes to answer a question the add already knew. Entries stored before this read as `false`, which is what the schema says, and `MenuStore.isAddOn` still asks the menu's card for them. |
| 451 | `SpecialtyIngredient.note` is decoded (leniently, defaulting to empty) and shown on the detail screen under the first store alternative, or with the choice when the ingredient has no store alternative at all | The note is the shopping caveat — an international-aisle product, a refrigerated one, or why there's no honest store equivalent — and #443 made it a rule that every curated entry has a store alternative **or** a note. Not decoding it meant the seven entries that carry one showed the store route with the caveat silently dropped, which is the one place it changes what a member buys. It belongs with the option it qualifies, so it sits under that option; when there is no store alternative the note is usually the explanation for that, so it goes where the choice is instead of vanishing. |
| 452 | `SpecialtyStore.settingsError` remembers a failed strategy load, and the summary row reads "Couldn't load — tap to try again" rather than "Loading…". The row stays, and the screen it opens keeps its Try Again | The setup screen deliberately ignores the throw so a failed strategy load can't block the ingredient list, which left the row with no way to know the load had ended: against a server without the route (a `404`) it showed a spinner and the word "Loading…" for as long as the screen was open. Saying so in the row is the honest version and costs nothing, since the destination already retries on appearance and offers Try Again. Hiding the row the way #421 hides the store catalog would be wrong here: the strategy is a setting the household has whether or not this request worked, so its row shouldn't disappear. |
| 453 | The recipe screen's tabs are **Customize / Description / Ingredients / Nutrition**. Customize (the protein swap and the "Tastes even better with" carousel) is offered only when the recipe has customizations or pairings, and a selection is corrected at read time by `RecipeDetailTab.available(hasCustomizations:hasPairings:)` and `resolve(_:in:)` when the extras load and change the tab set | The old Overview tab stacked the protein swap, the add-on carousel, the description, utensils, ratings, Autopilot and order history into one column — the dense single column the owner keeps calling clunky — and it buried the two controls that change what actually arrives under reading material. Splitting on "changes the meal" against "describes the meal" is what makes four tabs mean something rather than four arbitrary piles. Customize is conditional because a recipe with no protein options and no pairings would otherwise open on an empty tab, and resolving at read time rather than storing a corrected selection follows the same rule the specialty flag and pantry statuses already use (#57): the extras request can empty the tab the screen already landed on, and a stale selection must not survive it. The label is the verb "Customize" because four segments have to fit one segmented control on a phone. |
| 454 | An `OnAccent` color pairs with `AccentColor` (white in light, near-black in dark) and everything that fills a shape with the accent writes `Color.onAccent` on it. The two badge styles that tinted text on a translucent backdrop — `PhotoBadge`'s prominent ⚡ capsule and `MenuChip`'s prominent chip — fill with the accent instead, and a plain photo badge moves from `.regularMaterial` to `.thickMaterial` | The accent lightens to `#6BC084` in dark mode, where hardcoded white is 2.2:1 against it — under half of WCAG AA, on the week strip's selected pill, the Menu's filter chips, the servings stepper, a card's add mark, and the Autopilot setup tiles. A paired asset color is the fix that can't drift: one place decides what is legible on the accent, and both appearances are checked by a test rather than by eye. The badges are the same failure in weaker form (about 4.3:1 light, 3.1:1 dark at 11–12 points) and worse placed, since they sit over photography whose brightness the app doesn't control — filling them makes the pair 5.1:1 and 8.1:1, and the thicker material keeps a plain badge from borrowing a bright photo's brightness. |
| 455 | The week strip's height comes from `WeekStripMetrics.height(for:)`, which adds up the three line heights the pill actually draws and stops scaling at `.accessibility1`, and the pills are clamped to that same size with `.dynamicTypeSize(...)` | A horizontal `ScrollView` takes all the height it is offered, and as a top safe-area inset that is the screen, so the height must be pinned — and a freely scaling `@ScaledMetric` turned the pin into the same bug from the other side: about 210 points at `.accessibility5`, a strip of chrome sitting above every screen in the Menu tab. Clamping the text and computing the reservation from the same clamp is what makes it safe: the pills can't outgrow the box that holds them, so nothing is clipped. Capping text in this one strip is acceptable because nothing lives only here — the shown week's dates are in the bottom bar at full size, the weeks repeat in Past Weeks, and VoiceOver reads each pill's name, dates, and counts at any text size. |
| 456 | `MenuCardMetrics.titleLines(for:)` returns `nil` at accessibility sizes, so a card's name wraps to as many lines as it needs and `CardTitle` reserves no fixed height there. Two lines and equal heights still apply at every other size | Equal card heights are a layout nicety; reading the name is the point of the card. At `.accessibility3` a carousel card is 300 points wide and a line holds three or four words, so a two- or three-line clamp truncated most real names to "One-Pan Santa Fe…" — the failure #447 fixed at the default size, reappearing where the text is largest and the reader is least able to guess the rest. It costs nothing visually: the carousels lay cards out `.top`-aligned and Your Meals already stacks vertically at these sizes. |
| 457 | `ServingsStepper`'s − and + are `ServingsStepper.minimumTapTarget` (44 points) square, and the capsule's own vertical padding shrinks so the control grows about 10 points rather than 20 | 24-point targets eight points apart are not something a shaky hand hits, and the card stepper is the control the Menu asks for most often. VoiceOver was already served here — the stepper is one adjustable element — so this is the motor half of the same control. Letting the buttons carry the height instead of adding padding around them keeps the capsule close to the size the card was designed around. |
| 458 | The Menu's jump to All Meals after a filter changes asks `MenuScroll.animation(reduceMotion:)`, which is `nil` under Reduce Motion | The jump itself is not the problem — landing on the new first page is the point, and without it the screen looks empty until you scroll back (#…). The sliding is the part someone turned the setting off for, and it was the one animation in the redesign that ignored it, while the toast and the strip's own scroll already honored it. |
| 459 | `recommendations.sameWord` compares the plural forms by length and suffix instead of building `want+"s"`, `want+"es"` and `TrimSuffix(want,"y")+"ies"` on every call. The menu still reads the catalog and scores it per request; nothing is cached | Measured against the owner's real catalog (431 recipes, `dinneros_sim`): `GET /menu` was 109 ms, and every All Meals page cost the same 109 ms however deep it was, because each page re-reads and re-scores the whole catalog to return 24 cards. The split said the read was not the problem — Mongo's find plus decode is 17 ms, building the sections 0.15 ms, and filtering and sorting a page 0.2 ms — while `newSnapshot` alone was 79 ms. A CPU profile put 80% of that in `containsWords`/`sameWord` under `runtime.concatstrings`: the three plural forms were allocated for every ingredient word against every phrase rule of every recipe, which is roughly a million throwaway strings per request. Comparing by length and suffix is the same predicate with no allocation, and it takes `newSnapshot` to 16.9 ms, `GET /menu` to 44 ms and a page to 42 ms. Caching the scored snapshot per household would win more, but it buys a correctness problem — every rating, cook, plan edit and profile change invalidates it — that 65 ms did not justify. |
| 460 | A screen that can't build its model says so with Try Again rather than spinning: the grocery list and Add Recipes both answer `nil` without a household, and now show a `ContentUnavailableView` instead of a `ProgressView`. `AutopilotStore` gains `refreshError` like every other store, and its `weekError` — set since it was written but read by nothing — is shown on the week's Autopilot section with Try Again. A `403` from a pantry change, a `MealPlanner` change, or the week's pairings reloads the household role, as the specialty screens and `ShopErrors` already did | The same failure as #452, swept for rather than fixed one screen at a time: state that means "this didn't work" being rendered as state that means "this hasn't finished", or not rendered at all. The two spinners were unbounded — nothing retried and nothing timed out, so the only way out was to leave the screen — and `weekError` was dead state, which is the worse version: the store knew the week's suggestions had failed and the rows below it still read as "nothing suggested", an empty state standing in for an error. `refreshError` is the existing name in `MenuStore`, `PlanStore`, `PantryStore`, `SpecialtyStore` and `ShoppingStore`, so Autopilot was the one store where a failed pull to refresh was silent. The `403` reload follows the same rule from the other side: hiding a control is a convenience the API doesn't rely on, but a role that changed elsewhere must stop failing every tap until the next launch. Deliberately left alone: the recipe screen's customizations and pairings, and the Menu's filter options, still swallow their errors and degrade, because those are extras an older server legitimately lacks — the rule #421 set for the store catalog. |
| 461 | A skipped week is a state the app renders, not a week nobody got to: `WeekAutopilotSection` shows "Skipping this week" in place of **Plan with Autopilot**, the ⋯ item is disabled, `WeekView`'s empty state reads "Skipping This Week — nothing planned, on purpose" with **Change This Week**, and a proposal with no slots is never offered for review. `WeekContextSheet` opens on three tiles (`AutopilotWeekPreset`: Busy, Guests, Away) above the exact numbers, and Away is exclusive | The API already modeled and consumed all three of the owner's cases — `busy` shifts the weeknights to the quick band and exempts a `timeBand: long` smoker rule, the week's and a day's `servings` reach the slot, the accepted entry, and through it the grocery list's quantities, and `skip` returns an empty week with `week_skipped` — so what was missing was only the app. Every surface still treated a skipped week as an unplanned one: the same "Plan with Autopilot" row and the same "Nothing Planned — add recipes from your library" empty state, which is the nag and the false failure the owner asked not to see; and planning anyway saved a *pending* zero-slot proposal, whose row read "Autopilot suggested 0 meals" and whose **Add** button could only fail with `ErrNothingToAccept`. Reading the tiles back from the draft rather than storing a preset keeps one source of truth — the week context the API already has — so a week set up from the detailed rows still lights the right tile, and Guests is on whenever the week *or any day* serves extra. Away is exclusive because a week with no meals can't also be busy or have guests, and a stored `busy` under a skip would otherwise light two tiles that contradict each other. |
| 462 | A skipped meal's `reason` reaches the planner and decides whether the skip is evidence about the meal: `autopilot.Interaction` gains `Reason`, the adapter forwards `RecipeSkipped.Reason`, and the baseline's `conversion` signal counts only skips the household didn't explain away — `ate-out`, `missing-ingredients`, and `no-time` are left out of both sides of the ratio, while no reason, `not-in-the-mood`, and `other` count exactly as before (`ModelVersion` → `baseline-2026.5`) | The app has asked *why* a meal was skipped since skips were added, the enum has always been validated (`SkipReasons`), and the answer was then thrown away: the adapter read `RecipeSkipped.Date` and dropped `Reason`, so every skip fed `conversion` identically. That made the one signal the household writes by hand actively misleading — a week the household ate out, or a night an ingredient was missing, read as "you don't cook this" and pushed a meal nobody had rejected down the next week's ranking, with `recency` already suppressing it for having been planned. Only the reasons that are about the meal should carry preference weight, so `not-in-the-mood` keeps its full negative and the circumstantial ones become neutral rather than being counted as mild dislike, which would just be the same error scaled down. `no-time` is deliberately grouped with the circumstantial ones even though it *is* about the meal: cook time already has `timeFit`, the day's soft limit, the weeknight limit, and the week's cook-time mix, so letting it also drive `conversion` would penalize a long meal twice and thin out precisely the long cooks the household plans for the days it has time for — the Sunday smoker night being the case that must not erode. The default stays backward compatible, which matters for the provider boundary: a tenant that sends no reason gets exactly today's behavior, so this is a signal a tenant may supply rather than one the contract now demands. |
| 463 | Import review items are readable: `GET /households/{householdId}/recipes/import-reviews` (permission `recipes.import`, `status=open` by default) returns each item oldest first with the `recipeId` it is about, resolved through `sourceAliases`. Nothing resolves or closes an item automatically | The collection was write-only. `Store` had `SaveReviewItems` and no read method, no handler mounted one, and no screen or command showed one, so the 93 items the household's import recorded had never been seen by anything — the importer was flagging what it could not map into a collection nobody could open. The 90 `variant` items are the ones that matter: the recipe page HelloFresh redirects a weekly clone to is sometimes a different variant than the box held, so the stored ingredients can name the wrong protein — `Chicken Sausage Cavatappi Bolognese` stores Italian Chicken Sausage Mix and was delivered as pork sausage on 10 of its 21 ordered weeks, which is what the grocery list, the allergens, and Autopilot's protein variety all read. Resolving one needs a capture of that delivered ID and a re-import, which `go run . variants` already lists and `capture` already fetches, so the gap was only ever the reading. `recipeId` is resolved rather than stored because clones of one dish merge under the newest source ID, leaving the item's own ID an alias; it is omitted rather than faked when no recipe carries the ID any more. The permission is `recipes.import` and not `household.view`: this is import bookkeeping, and the person who can fix it is the person who can import. Deliberately not done: no severity field and no auto-resolve. Roughly half the `variant` items are cosmetic — "honey butter cornbread" against "Honey Butter Corn Bread" — and a second name-matching heuristic in the API to hide them would be guessing where `sameVariant` in the importer is the one place that comparison belongs. |
| 467 | The Shop tab follows the plan the way the Menu does: `PlanStore.planDidChange` now reaches `ShoppingStore.planDidChange` as well as `MenuStore.applyPlan`, which re-matches the shown week and bumps `planRevision`, and `ShopWeekList`'s export section is keyed on `ShopListKey(week:planRevision:)` instead of the week alone. `PlanGrocerySignature` decides what counts as a change: entry ID, recipe, servings, and customizations | Everything downstream of the match — the Ready rows' package counts, the cart links, and the plain-text export — is computed from the week's grocery list, so a serving size changed on the Menu changes every quantity on this tab. Nothing asked again: the tab's `.task` is keyed on the household, `load()` deliberately keeps a proposal it already has, and a `TabView` keeps the Shop tab alive, so switching back re-ran nothing. The failure was silent and expensive in the one direction that matters — the export and the cart are what the household actually shops from, so the tab quietly handed over last week's amounts. Reusing `planDidChange` rather than adding an observer keeps one path for "the server returned a plan", which is already what patches the Menu's cards and the strip's counts. The signature is the part worth arguing: `planDidChange` fires on every load of every week, so comparing whole plans would re-match on arrival, and comparing nothing would put a network POST behind a note or a drag to another day. Only what the grocery list is built from is compared, which is also why a day move is deliberately excluded — the list aggregates across the week and doesn't care which night a meal lands on. The first plan for a week never re-matches, because that's the one the tab's own load is already matching. |
| 464 | A pantry row whose estimate had to skip a cooked recipe shows an orange `exclamationmark.circle` after "~31% left", and says "some use not counted" to VoiceOver (`PantryUsageFormat.skippedRecipesShort`). No `skipReason` was decoded, because the API does not expose one | The audit's description did not survive contact with the code, and the honest version is narrower. `skipReason` exists only inside the API — `pantry.CookSkipReason` and the `skipReason` field on `pantry_cook_usage.lines[]` in Mongo. It appears in no response schema: `grep skipReason api/openapi.yaml docs/api.md` finds nothing, and no mounted route returns a cook-usage record at all (the pantry routes are the item, bulk, staples, purchases and settings). So there was nothing for iOS to decode, in one detail screen or anywhere else, and adding a decoder would have been inventing a field. What *is* real is the same failure one level up: `estimate.skippedRecipes` — the count of cooked recipes that used the item and couldn't be deducted — is decoded, and was rendered in exactly one place, `PantryUsageSections`, which is the item detail. The Pantry list itself showed the ring and the percentage with nothing to distinguish an item whose use was fully counted from one whose Salt line was recorded as `not_tracked`, which is the case the percentage most misleads about. The fix is at the row, where the number is actually read. Surfacing the reason itself needs an API change and is deliberately not done here. |
| 465 | `RecipeFormat.isPlural(_:)` is the one pluralisation rule — plural when the value is zero or above one, singular at exactly one and below it — and `RecipeFormat.amount`, `PantryFormat.amount` and `PantryUsageFormat.amount` all ask it instead of testing `value > 1` themselves | Three call sites had independently written `(value ?? 0) > 1`, which reads zero as singular: an item used up rendered "0 cup". English only makes *exactly* one singular, so zero is plural and fractions below one are not — "0 cups", "½ cup", "1 cup", "1½ cups" — which is also what keeps "1 package = ½ cup" reading correctly on the package-size row. Making it one function rather than fixing the comparison three times is the point: the bug was the rule being restated, and a fourth call site would have restated it again. `nil` stays singular, preserving today's behaviour for an amount with no number to agree with. |
| 466 | The item detail's Other Use row shows `estimate.otherUse` — the non-recipe amount applied this cycle — with the daily rate demoted to its caption, and `PantryUsageFormat.dailyRateBasis` ("Learned from 3 earlier periods") is deleted rather than kept as a third line | The row was showing `dailyRate` as its value, which is a different quantity in a different unit-per-time: an item five tablespoons down read "About 1 tbsp a day" under a heading promising how much other use there had been, while `otherUse` was decoded, carried through every fixture, and displayed nowhere in the app. Reading the row as a whole settles which number it wants — it sits between "Recipes: 2 recipes used 6 tbsp" and the threshold, so it is the non-recipe half of the same accounting, and the API's own `summary` pairs them the same way ("2 recipes used 6 tbsp, plus about 1 tbsp a day of other use"). So the row is not removed: it is the only place the other half of the estimate is stated. The rate keeps its place as the caption, because it explains where the amount came from. The basis line goes because three stacked lines in a `LabeledContent` is the dense-text failure the owner has asked twice not to see, and "learned from 3 earlier periods" is the least load-bearing of the three — it explains the confidence of a number that is itself now only a caption. |
| 468 | A meal card in Your Meals whose night has passed shows "How was it?" and five stars, and one tap saves that score (`MealRatingRow`, gated by `MealFeedback.hasHappened`); the saved rating is patched onto every card for that recipe through `MenuStore.applyRating`. Tapping a star carries an existing comment and tags through untouched, and nothing is marked cooked | Ratings are the heaviest signal the planner has — weight 0.30, with rating tags at 0.20 and `never-again` a hard filter — and the household's database holds one rating, three cooked events and no skips across 431 recipes, so weeks are built almost entirely from order history and the taste profile. The machinery was never the problem; the asking was. Rating lived in exactly one place: `RecipeRatingSection`, mounted inside `RecipeDescriptionSection`, which is the Description tab of the recipe screen — past a 240–420pt hero, the title block, the stats row and the tag chips, then a star tap plus a second deliberate Save. Nothing on the Menu, on a card, or on a week row mentioned that rating existed; `HouseholdRatingSummary` renders "No ratings yet" as plain text with no affordance to change it. Cooked and skipped were worse off, reachable only through a swipe or a long-press, both invisible, and `EventReporter.outcomes` keeps them in memory for one launch, so the badge is gone after a relaunch. Nothing prompts at the moment a meal happens, and nothing can: there is no notification scheduler in the app, and `pantry.low` is the only notification type. So the fix had to be a surface, not a reminder. The meal card is the one thing already on screen with no taps at all, it already carries `myRating` and the household average from the menu response, and after its night has passed "how was it?" is a question about something that happened rather than a prompt about a meal still to come. The gate is deliberately narrow — past weeks entirely, the current week up to and including today, never an upcoming week, and never a meal planned without a day, which has no night to ask about — because a card that asks early reads as nagging, which the owner would rather not have than have feedback sooner. The score alone is written and the draft is seeded from the existing rating, so changing a 4 to a 5 from a card can't discard a comment someone typed on the recipe screen; tags, comments and removal stay there. Deliberately not done: the tap does **not** record `recipe.cooked`. It would be the obvious second win, since cooked drives pantry deduction and the conversion signal, but a rating is a statement about the food and not a statement that this household cooked it that night, and inferring the second from the first would write exactly the kind of event the planner must not learn from. Making cooked and skipped discoverable is the same argument one step further and is left for its own change. |
| 469 | The import review backlog is readable in the app: a screen under the Household tab's **Imports** section, gated on `recipes.import` and hidden entirely when the endpoint answers `404`. It groups the `variant` items by stored recipe, leads with the ones whose delivered name isn't the stored name respelled (`ImportVariantMatch.isSpellingOnly`: lowercase, fold accents, drop everything that isn't alphanumeric), folds the rest into a collapsed disclosure, and offers no way to resolve anything | #463 made the 93 items readable over HTTP and nothing read them, so the backlog was still invisible to the person who can fix it. The screen's whole job is letting the owner eyeball the ~47 genuine protein divergences without the ~43 capitalisation differences drowning them, which is why the sort is the feature and the list is deliberately short on text. Doing that comparison in the client is a heuristic, and #463 refused to put one in the API for good reason — `sameVariant` in the importer is where that comparison belongs — so this one is conservative in the direction that can only fail safe: it hides a row only when the two names are identical once case, spacing, punctuation and accents are gone, and anything it can't match, including a name it can't read, stays in **Box Looked Different**. One real difference keeps the whole recipe out of the folded pile. The screen says in its own footer that the split is its guess, because a heuristic the reader can't see is one they can't correct for. The framing is "on at least one delivered week the box differed" and never "this recipe is wrong": a recipe is a merge across many deliveries — `Chicken Sausage Cavatappi Bolognese` was correct on 11 of its 21 ordered weeks — so a per-recipe verdict would be false for most of them. `flaggedDeliveries` counts flagged clones and is deliberately not called weeks: the item carries no week, and `orderWeeks` is every week the recipe was received, not the ones that diverged, so attributing weeks would mean inventing the attribution. No resolve affordance exists because no endpoint closes an item — a fake one would be the worst outcome here, since the item would still be open after the tap — so the screen names what resolving actually takes (`go run . variants` and `capture` in importers/hellofresh, then a re-import). The permission is `recipes.import`, matching the endpoint rather than the tab it sits in, and the `404` hiding follows #421: an API without the route shows no broken row. The badge counts only the differences, because a spelling item is not something to act on. |
| 470 | A meal card whose night has passed asks "Did you make it?" with **Cooked** and a **Skip** menu of reasons (`MealOutcomeRow`, gated by the same `MealFeedback.hasHappened` as the stars and sitting above them), and the answer is durable: `EventReporter` keeps outcomes in `EventQueueSnapshot.outcomes` beside the queue, so a Cooked badge survives a relaunch. Tapping Cooked holds the event for `EventReporter.undoWindow` (8s) behind an **Undo**, which deletes the queued event outright | Cooked and skipped lived only in a long-press menu and a swipe, and nothing on screen said either existed: 3 cooked events and 0 skips ever, against a `conversion` term the Autopilot scorer weights at 0.10 and the pantry deduction that only a cooked event triggers. An unrecorded night is lost signal, and skip reasons became worth collecting once circumstantial ones (`ate-out`, `missing-ingredients`, `no-time`) stopped counting against the conversion ratio while `not-in-the-mood` and `other` kept counting. Discoverability alone would have shipped broken, though, because `EventReporter.outcomes` was an in-memory dictionary described as "for this launch": the event carrying the answer is deleted the moment the API accepts it (#90), so the answer died with it and the app could not tell anyone what they had already marked. Outcomes are therefore stored next to the queue rather than in it — the two empty at different times — decoded with `decodeIfPresent` so a queue written before this change keeps its pending events instead of being dropped by a version bump, and pruned at 120 days rather than the queue's 29 (#76), because that limit is the API's rule about event age while an outcome is a local record the Menu still shows for an old week. The row follows #468's argument one step further and inherits its restraint: it is two buttons on a card already on screen, it disappears once answered because the photo already carries the badge, and nothing infers an answer — a week going by does not cook a meal and neither does rating it. Undo exists because a plain button is much easier to hit by accident than a long-press, and cooking deducts the pantry the moment the API stores the event (#110); holding the send for 8 seconds makes Undo a local delete of a queued event rather than a reversal the API has no endpoint for, and the event still sends on the queue's own schedule if the card scrolls away. `WeekView`'s swipe keeps sending immediately: a directional gesture is hard to trigger by accident, and it was already shipped that way |
| 471 | `MealPlanner` is scoped to a household like every other store: `activate(householdID:)` drops the toast, its undo, the busy sets and `errorMessage` when the household changes, and `MenuView`'s existing `.task(id: household?.id)` calls it alongside `library`, `menu` and `plans` | `RootView` resets the twelve stores from `.task(id: session.currentUser?.id)`, which fires only when the signed-in user changes, so a *household* switch by the same user reset nothing — and `MealPlanner` was the one store with no scoping of its own to fall back on. Every other store already guarded itself: `PantryStore`, `SpecialtyStore`, `PlanStore`, `RecipeLibrary`, `MenuStore`, `ImportReviewStore`, `NotificationStore`, `ShoppingStore` and `AutopilotStore` all clear when `activate` is handed a different household, and `PairingsStore.showWeek` does the same with a test for it; the planner had `reset()` alone, whose comment claimed "for sign-out and household switches" while nothing ever called it for the second. The leftovers are the user-visible half. `MenuView` renders `planner.toast` as an overlay and `RecipeDetailView` renders it again, and the Menu tab is keyed `.id(household.id)`, so switching households rebuilt the tab and re-ran `PlannerToastView`'s `.task(id: toast.id)` — the other household's "Added Skillet Tacos · Undo" reappeared over the new household's menu for a fresh five seconds. Tapping Undo was the part that actually wrote: `undo()` guarded only `toast.week == plans.week`, and both households default to the current week, so the guard passed and the undo was sent against the household now shown — a `.remove` deleting an entry ID that household never had, and a `.restore` trying to add another household's recipe to this one's plan. Both fail at the API, which is the point: the isolation holds server-side and the damage is a "Couldn't Change Your Meals" alert for something the user didn't do. `errorMessage` survived the same way and popped that alert on arrival. Scoping the store rather than filtering the toast on read is deliberate: a computed `toast` that hid another household's entry would bring it back on switching home again, and mutating stored state from an `@Observable` getter to avoid that is the kind of read-side write that makes observation loops. Stamping `householdID` onto `Toast` and guarding `undo()` was considered and dropped as redundant once the store clears on switch — it would add a field to a struct whose memberwise init is used in two places, for a case that can no longer arise. |
| 475 | An ingredient a household never wants is its own third state, not a pantry item and not a check-off: `internal/skips` owns `grocery_skips`, unique on `{householdId, ingredientKey}`, with exactly two lifetimes (`week`, `always`). A skipped item is aggregated like every other line and then held back in `grocery.List.SkippedItems` with `StatusSkipped`, never dropped, and the week's response carries it in `skippedItems` beside the existing `skipped` | The owner's ask was "skip once, or skip forever", and the temptation was to reuse something that already exists. Neither fits. The pantry's "already at home" answers *do you have it* — an ingredient the household owns is `inPantry` and comes back onto the list the moment the item is marked `out`, which is exactly wrong for cilantro, since the household will never have it and never wants it. Check-off answers *did you buy it*, is per week, and lives in `UserDefaults` on one device. Storing "never buy" as a permanently-in-stock pantry item would have been the cheap route and would have corrupted the pantry's own meaning: usage tracking would estimate it down, the low-stock check would raise a notification about an ingredient nobody intends to own, and `POST /pantry/staples/defaults` reasons over the same collection. So three states, three places, and the API doc states the distinction in a table because the app has to render them differently. Holding skipped items back rather than dropping them is the other half: the recipe still calls for cilantro, and a list that silently omitted it would make the recipe screen and the grocery list disagree about what a meal needs — the household would find out at the stove. Returning them with their amounts, recipes and `via` intact means the review screen can say what is being skipped and which meals wanted it, and it costs nothing downstream, because everything built from the list reads `categories`: the Walmart handoff in `shopping.buildProposal` iterates `g.Categories` and so excludes them with no change of its own. The unique key is load-bearing for the two lifetimes: posting again replaces, so "skip once" becomes "skip forever" without a delete-then-create and without two rows to reconcile. Week skips are never cleaned up — a past week's skip just stops matching — because the alternative is writing to the database as a side effect of reading a grocery list, and because opening an old week should still show what was skipped then. |
| 476 | Skips resolve after `ApplySpecialties`, against the keys the list actually ends up with, and are matched under three spellings (the key the skip was made from, `name:<normalized name>`, and the catalog ID resolved from that name). They are deliberately **not** wired into Autopilot | Both halves are about a skip meaning the *ingredient*, not the line it was tapped on. A recipe can reach cilantro through a catalog ID while another names it as free text, and the pantry already solved that with exactly these three keys (`pantry.GroceryPantry`), so a skip that only matched the tapped line would leave cilantro on the list from the next recipe that spells it differently — the household would skip it again and reasonably conclude the feature is broken. Resolving after specialties is the same argument for a case that is easy to get wrong: a store alternative expands one specialty line into several ordinary ingredients, so the cilantro on the list may exist only because "Tex-Mex Paste" was replaced by its components. Running skips before that expansion would mean skipping the paste, which is not what was asked; running them after drops just the cilantro, leaves the chili powder, and keeps the `via` that explains where it came from. There is a test for precisely that path. Autopilot is left out on purpose, and this is the judgement call worth recording: a forever-skip *is* mild evidence against recipes built around that ingredient, and the scorer would happily take it. But the owner asked for a way to not buy something, not for meals to start disappearing, and Autopilot already has explicit restrictions and `never-again` for "don't show me this". A hidden filter would remove recipes the household never rejected, with no screen saying why — the same failure as #462, where a signal collected for one purpose silently reshaped the week. Surfacing it later ("you skip cilantro; 4 of these meals are built on it") is the honest version and needs a screen, so it stays out of scope rather than going in quietly. |
| 472 | Developer sign-in picks who it signs in as: a subject field defaulting to `dev-simulator` plus a menu of the audit's test subjects, with the email and display name derived from the subject by `DeveloperIdentity` (`rachel-sim` → `rachel-sim@example.com`, "Rachel Sim"). The field, the menu and the type live inside the same `#if DEBUG` and `environment == .development` gate the button already had | The two-person household works over HTTP and had never been exercised in the app, because `SignInModel.developerSubject` was a hardcoded `"dev-simulator"` with a fixed email and name: the button could only ever be one person, so a second member's first run — accepting an invitation, seeing an empty week, the pairing screens — was unreachable from the UI. The API already did the work: `POST /auth/dev` takes a subject, `FindOrCreateByIdentity` looks users up by `(provider, subject)` alone, and an unseen subject creates a user and answers `isNewUser: true`. Nothing about the gate changed, which is the point — the affordance sits inside the existing `#if DEBUG` block that is itself inside `configuration.environment == .development`, so it is absent from Release binaries and from any Debug build pointed at production, exactly as before. The email and name are derived rather than typed because three fields on the sign-in screen is the clutter the owner objects to, and because test users all called "Simulator Developer" are indistinguishable in a household member list, which is the screen this change exists to test. Derivation sanitises the address (`validEmail` on the API rejects anything it doesn't like, which would fail a sign-in that would otherwise work) and falls back to the default subject when the field is blank or is punctuation alone. A pre-existing user keeps the display name it was created with, since `signInExisting` updates the email and never the name, so an old `dev-simulator` still reads "Simulator Developer"; that is a database fact, not a bug in the derivation. No password or token is stored — dev sign-in has neither |
| 473 | The Menu's week strip collapses as you scroll down and returns as you scroll up. `MenuChromeCollapse` decides, from content offset alone, between the pills and a one-line bar naming the shown week (`WeekStrip(isExpanded:onExpand:)`), whose height comes from `WeekStripMetrics.collapsedHeight(for:)` exactly as the pills' comes from `height(for:)` (#455). It is always expanded within 8 points of the top, needs 40 points of downward travel to collapse but only 24 upward to return, measures travel from the turning point, and is held expanded across the filter jump to All Meals (#458). Under VoiceOver it never collapses; under Reduce Motion the change is not animated (`MenuScroll.collapseAnimation(reduceMotion:)`) | About a quarter of a phone screen was chrome before any food, and the photos are the point. Only the strip collapses: the Menu/By Day picker is the navigation bar's, and All Meals' chips are what you reach for *while* scrolling that list, so hiding either trades the wrong thing. The week itself is what the rest of the screen is about, so the compact form names it rather than vanishing, and tapping it brings the pills back. Travel from a moving anchor rather than absolute position is what stops jitter toggling it, and the two thresholds differ so that where they could disagree the strip shows — the owner's rule. VoiceOver is excluded outright: collapsing can strand focus on an element that has gone, and screen space is not what element-by-element reading is short of |
| 474 | Choosing a Walmart product takes the ingredient's grocery category into account three ways: `shopping.SearchTermsFor` biases the suggested search by category (produce searches "fresh whole …"), `Walmart.ParseProduct` reads the product's name and often its package size out of the link's own slug, and `providers.CountPackagesFor` adds a `per_week` coverage rule so a bulb covers a week of cloves. All three are defaults a member can override | The owner tested the flow and hit three separate walls, and the fixes share one source of truth — the category already on every grocery line — rather than a table of ingredients. **Search:** tapping "Search on Walmart for garlic" ranks garlic powder, garlic salt and garlic snacks above the bulb, and the product he actually wanted is titled "Garlic Bulb Fresh Whole, Each". The bias therefore belongs to the aisle, not the ingredient: produce wants fresh and whole, spices want the dried jar, frozen wants the freezer, and pantry and condiments have no single right form so they get no qualifier at all — an absence that matters, because guessing there would make the search worse. The one cross-cutting rule replaces what would otherwise be a pile of exceptions: a name that already states a form ("Garlic Powder", "Minced Garlic", "Frozen Corn") is searched as itself, since the recipe asked for that form on purpose. Matching is whole-word, so "chipotle" isn't read as "chips". The `avoid` list ships in the response and is shown to the member as a hint rather than used as a filter, because filtering needs a search API that does not exist yet — stating that honestly is the point. **Name and size:** the link already carries both, and the API was throwing them away and then demanding the member retype the name, which is what he objected to. `/ip/Garlic-Bulb-Fresh-Whole-Each/123` gives "Garlic Bulb Fresh Whole Each" and 1 ct, from the URL string alone with nothing fetched, so `displayName` is now required only when the link has no slug. The slug is lossy and the code says so: hyphens become spaces so the comma in the real title is gone, and a decimal written as two numbers (`-2-5-lb`, which is 2.5 lb or 5 lb with no way to tell) yields **no** size rather than a wrong one — the one case where inventing data would be silently expensive. iOS derives the name a second time so the field fills as the link is pasted and can be edited before saving; the API stays authoritative. **Coverage:** a recipe asking for 1 clove against a bulb sold by the each could not be measured, so the line was flagged "check amount" every week — noise, for the case with the obvious answer. `per_week` assumes one package covers the week and says so ("1 × 1 ct covers this week (4 cloves)"), and it deliberately changes only the un-measurable row of the package-count table: 2 ¼ lb of beef against 16 oz packages is still 3 packages, because collapsing a need that *can* be measured would under-buy it, and a partly measured line keeps its exact count and its flag. The default follows the category — the fresh five assume no carry-over, pantry and spices are counted exactly — and the resolved rule is stored on the handoff line so an old handoff recomputes the count it was created with. The owner's "fresh produce lasts only that week" needed no mechanism: each week's list is rebuilt from that week's recipes, so next week asks for the bulb again on its own. Deliberately not done: suggesting easier equivalents (jarred minced garlic for a clove). It needs a product catalogue to find the jar and a per-ingredient equivalence ("1 clove = ½ tsp jarred") that nothing in the repo holds; `internal/substitutes` is where it would live once walmart.io keys arrive, and faking it from a hardcoded pair or two would be a demo, not a feature |
| 477 | The grocery order day is a household setting (`orderDay`, `mon`–`sun`, unset by default and unset meaning no reminders), and the weekly reminder is **derived on read**, not stored and not scheduled. `shopping.Service.OrderReminder` answers "has the order day arrived, and is this week still unmarked?" from three things it reads at that moment: the household's `orderDay`, today's date in the household's time zone, and the week's ordered marker. `shopping.Service` implements `notifications.Refresher`, so reading the bell brings the reminder up to date and creates at most one notification per week, deduped on `shopping.order_due:<week>` | The word "reminder" suggests a scheduler, and building one would have been the wrong instinct: there is no cron, no background worker, and no APNs sender in this repo (#103 already chose derive-on-read for the pantry's low-stock check for the same reason, and the same Heroku Scheduler sweep is still the thing that would have to exist first). A stored reminder row buys nothing here and costs correctness — it would have to be written by something, rewritten when the household changes its order day or time zone, and reconciled when a week is marked, and any of those going wrong leaves a row that is quietly wrong for a week. Derived, the question cannot drift: it is recomputed from the household's own settings on every read, changing the order day changes today's answer immediately, and re-reading is free of side effects. Deduping on the week rather than the day is what makes it one reminder instead of a daily one — the owner's objection is to an app that pesters, so the notification is created once and then persists unread until acted on, and reading notifications all day adds nothing. The reminder also belongs to its own week: it starts on the order day and stops when the week ends, so next week starts fresh rather than a run of unmarked past weeks all asking at once. Supporting a second producer meant `notifications.Service` holding a list of refreshers rather than one (`SetRefresher` replaces, `AddRefresher` appends), each bounded by `RefreshTimeout` and each logged on failure, so one producer failing can't stop another or the read |
| 478 | Whether a week was ordered is the one thing stored (`shopping_order_weeks`, unique `{householdId, week}`), it is set **only** by a person through `PUT .../shopping/weeks/{week}/order`, and `{"ordered": false}` takes it back | Everything else about the reminder is derivable; this is not. Nothing in the system knows whether an order was placed: the Walmart hand-off opens a cart link and DinnerOS never learns what happened next (#204 exists precisely because Walmart reports nothing back), so inferring "ordered" from a created handoff, from opened cart links, or from a confirmed "Did you order these?" would silence the reminder for a household that opened Walmart and never checked out — the exact person the reminder is for. That is why the hand-off flow only ever *offers* the mark (#479) and never sets it. Undo is not a nicety: "Mark as Ordered" is a single tap on a banner that sits above the list, a mis-tap is easy, and without undo it would cost the household its reminder for the rest of the week with no way back. Marking is idempotent and keeps the first member and time (`$setOnInsert`), so a double tap doesn't rewrite who ordered, and unmarking a week that was never marked is not an error. Marking also marks that member's copy of the `shopping.order_due` notification read; unmarking deliberately leaves it read, because they did see it and an unread badge reappearing is its own kind of nagging |
| 479 | The Shop tab is the reminder's primary surface — a banner at the top of the week's list, directly **above** the "Did you order these?" banner — and the bell carries the same reminder as a `shopping.order_due` notification whose subject is `{kind: "shopping_week", id: "2026-W38"}`, which opens that week in Shop | The reminder is about ordering, and Shop is where the list is sent, so that is where the one control that resolves it belongs; putting the button anywhere else would mean reading the reminder in one place and acting on it in another. Its position is the answer to "is finishing a Walmart hand-off a natural moment to offer the mark?" — it is, and sitting directly above the hand-off banner means both are in view at that moment without a second prompt, a new sheet, or any change to `OrderConfirmationSheet`, which stays a question about what went in the pantry rather than a question about whether an order was placed. The bell is not redundant: it is where notifications already live (#108), it is what a member sees when they aren't in the Shop tab, and it is the surface that would carry a real push if one ever existed. Neither surface implies push does exist — it does not, for any notification type, and what real push would still require (an APNs auth key only the owner can create, the `aps-environment` entitlement, a permission prompt, device-token registration, and a sender plus the sweep that derived-on-read reminders need to fire for a closed app) is written down in pantry-usage.md rather than left as an assumption |
| 480 | A specialty's `unitSizes` may give the same discrete unit one size per kind ("1 count = 1 oz" and "1 count = 2 tbsp"); `grocery.ConvertMeasure` tries each size for the unit and uses the first whose kind matches. Seed parsing still rejects two sizes of the same kind for one unit | Garlic Herb Butter is listed on recipe cards as either "1 ounce" or "2 tablespoon", and with one size per unit a store option could scale only one of them. Both sizes describe the same packet, so this is an exact packet definition rather than a density guess, and #98 still holds: a bare "1 oz" never becomes tablespoons except through the packet. The pantry's cook deduction already tried every resolved size in turn. |
| 481 | Real push is an hourly **sweep command**, `cmd/sendreminders`, run by Heroku Scheduler on a one-off dyno from the same image, not a goroutine in the web server and not a send inside the request that creates a notification. Each run pages through every household, runs the refreshers a notification read runs (`notifications.Service.Refresh`: the pantry's low-stock check, then the order reminder), and then pushes pending notifications from the outbox | The reminders are derived on read (#103, #477), so a closed app produces nothing; something has to do the read, and a scheduled command is the cheapest thing that runs while nobody uses the app. In-process timers on an Eco web dyno stop when it sleeps and would run once per dyno if it scaled. Sending at creation time would split delivery into two paths (created by a request vs. by the sweep) and push midnight reminders at midnight. One path through the outbox means one place that decides quiet hours, recipients, and exactly-once. The command wires only the refreshers and their stores rather than the whole server graph: `Refresh` needs nothing request-scoped, and a comment in the command names the server wiring it mirrors. Hourly is the finest grain the reminders have — the order reminder is per day and estimates move slowly — so a 10-minute schedule would cost dyno time for nothing |
| 482 | A push goes out **at most once**: the sweep claims a notification with one conditional update (`push.status: pending` → `sending`, `$inc attempts`) before sending, and whatever happens next is final — `sent` if any device accepted it, `failed` if every send failed, `skipped` if nobody was left to send to, it is over 24 hours old, or its condition ended while it waited (`push.Relevance`: shopping says the week was ordered, the pantry says the item was restocked or deleted). Nothing is retried and nothing returns to `pending` | The owner's objection to reminders is being pestered, and a duplicate alert is worse than a missed one: a missed push still waits on the bell, which is the product without push. Retrying after a partial failure can't be made safe per notification — the devices that got it would get it again — and per-device outbox rows would be machinery for an app with a handful of phones. The claim is what makes overlapping or repeated runs harmless, so the Scheduler job needs no locking. A sweep killed between claim and finish leaves `sending` forever, which is the at-most-once choice made visible rather than hidden. The 24-hour cutoff keeps the first deploy from pushing every notification created before push existed, and keeps a household that was in quiet hours for long from getting stale news |
| 483 | Recipients are the household's **current members who haven't read the notification**, on every device registered to them; between **21:00 and 08:00** in the household's time zone a notification waits (`pending`) for a later run. Device tokens are stored per user (`device_tokens`, unique token, with the APNs environment), not per household, registered with `PUT /api/v1/me/device-tokens` and removed by `DELETE` at sign-out or by the sweep on `BadDeviceToken` / `410 Unregistered` | Resolving members at send time means leaving a household needs no token cleanup, and a member with two households gets both households' alerts on one registration. Skipping members who already read it on the bell is what "new" means here: a member who already saw it on the bell doesn't need it again, and once anyone marks the week ordered the relevance check (#482) stops it for everyone. Quiet hours are the household's, because the order reminder becomes due at local midnight and would otherwise buzz at midnight; 8 a.m. is when "today is order day" is useful. Keying by token lets a shared device move to whoever signed in last, and storing the environment with the token is what lets one APNs key serve Xcode debug builds (sandbox) and TestFlight (production) from one deployment. The token travels in the body, not the path, so request logs never hold it |
| 484 | The APNs sender is `push.Sender` with one real implementation, `APNsClient`: token-based auth (an ES256 JWT with `kid` and `iss`, signed with the `.p8` from `APNS_AUTH_KEY`, cached for 45 minutes and re-signed once on `ExpiredProviderToken`) over Go's HTTP/2 client, choosing `api.push.apple.com` or `api.sandbox.push.apple.com` per token. `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_AUTH_KEY` are all set or all empty; empty makes the sweep a logged no-op that still refreshes, and the web server never builds a sender | Token auth needs no yearly certificate renewal and the one key works for both gateways. APNs rejects provider tokens older than an hour and throttles ones refreshed more often than every 20 minutes, so 45 minutes sits safely between. No third-party APNs library: `golang-jwt` is already a dependency and the protocol is one POST, so a library would add surface without removing code. Tests run the real client against local HTTP/2 TLS servers standing in for both gateways (checking the protocol version, headers, payload, and that the JWT verifies with the key's public half), and the sweep's tests use a fake sender, so no test can reach Apple. Validating the key at config load follows the Walmart IDs' all-or-none rule: a half-configured push should fail loudly at deploy, not silently at 8 a.m. |
| 485 | iOS asks for notification permission **only** after a member saves an order day in Household Settings, or taps **Notify Me on Order Day** on the Shop tab's order reminder — never at launch. Once allowed, every sign-in and foreground registers and `PUT`s the token (once per launch, replacing a rotated one); sign-out `DELETE`s it through `AuthSession.willSignOut` while requests are still authorized, waiting at most 3 s. A tapped push is held in `PushNotificationStore.pendingRoute` until `MainTabView` opens it where the bell row would — switching household first when needed — and `AppDelegate` owns `AppDependencies` so a push that launches the app is caught | A permission prompt at first launch is refused by people who don't yet know what the app would notify about, and iOS never shows it again. Picking an order day is literally asking to be reminded, so it is the moment the prompt explains itself; the Shop banner covers members who can't change household settings but see the reminder. A refusal changes nothing else. Deleting the token before the session is cleared is the only time the request can be authorized; a sign-out forced by a rejected refresh token can't, and a device left registered that way moves to the next user who signs in on it or is removed when APNs rejects it. Holding the route in a store rather than acting in the delegate lets the tab shell open it after households load, so a cold launch from a push lands in the same place as a tap on the bell |
| 486 | The HelloFresh importer reads each delivered recipe from the most exact source it has: an account capture, then the recipe's **printed card** (`go run . cards` fetches `recipecards/card/<deliveredId>.pdf`, then `-en-US.pdf`, sequentially 3 s apart, caches PDFs and misses under git-ignored `data/raw/cards/`, and parses them with a small in-repo PDF text extractor), then the public page. A card that is a different dish from the page its ID redirects to becomes **its own recipe** under its delivered ID and card title; one whose shipped ingredients match the page merges as a clone. Recipe names compare with case, accents, spacing, punctuation and `&`/`and` set aside and nothing else. The import gives a stored recipe to the file recipe that matches it most closely (same canonical ID, then its canonical ID listed as an alias, then most shared IDs); an alias-only loser is created, and every stored recipe gives up the aliases and order weeks the file assigns to another recipe. Nothing is deleted | 36 of the household's recipes were rejected as `matches the same stored recipe` because an earlier import had merged pork and beef deliveries under one stored recipe, and 47 `variant` items said the stored details might not be what arrived. The public page can't answer that, account captures only exist for recent deliveries, but every delivery had a printed card. Cards carry exactly what the box held — ingredients with 2- and 4-person amounts, steps, times, calories — and nothing else, so a card-derived recipe borrows only what doesn't change with the protein (cuisines, tags, difficulty, the page's IDs and images for same-named ingredients) and never the other variant's photo, description or nutrition. Unparseable or missing cards leave the review item in place: older cards live under hashed names that can't be derived from the delivered ID, so most pre-2023 variants stay flagged rather than guessed. The extractor and card parser are ~1,800 lines of standard library instead of a module because the only pure-Go candidate rejected these files (a trailing NUL after `%EOF`) and the cards need positions, not a text dump; it refuses layouts it doesn't recognise (amounts that don't pair with the serving columns, steps not numbered 1..n) and excludes the HelloCustom swap box and call-outs. Merging clones on respelled names is the importer doing what the app's `ImportVariantMatch.isSpellingOnly` (#469) guessed at; anything reworded ("molten", "Swissburgs") is still a different name, and is only merged when a card proves the same ingredients. On re-import the stored recipe keeps its ID, so plans and ratings keep pointing at it; a split delivery starts unrated. A stored recipe that was only ever a canonical page for deliveries now matched elsewhere is kept with no aliases and `timesOrdered` 0 rather than deleted, because other modules may refer to it. `pick` (one item picked into the box) is a count |
| 487 | New households get a **starter recipe library**: when `STARTER_RECIPES_HOUSEHOLD_ID` is set, `households.Service.Create` tells a `CreatedListener` (`recipes.Starter`), which copies that household's recipes into the new one **in a background goroutine**, holding the create response at most 3 s for it to finish (`DefaultStarterHold`), in pages of 100 (`ListRecipesAfter`, one `FindRecipesBySourceIDs`, one unordered `SaveRecipes` each). Only recipe content is copied; `orderWeeks`, `timesOrdered` and `lastOrderedWeek` are cleared, and no other collection (ratings, plans, pantry, import reviews, events, Autopilot overrides) is read. A source recipe the target already has from the same `source` by `sourceRecipeId` or alias is skipped, so `cmd/seedstarter -household <id> [-apply]` (dry run by default) backfills existing households and finishes failed copies without duplicating. A failed copy is logged and never fails creation; shutdown waits for running copies within the grace period | Friends and family who sign up would otherwise open an empty Menu and an Autopilot with nothing to pick from, and the owner's imported library is what they'd want. Copying rather than sharing a global catalog keeps recipes household-scoped, so every module that reads recipes (menu, Autopilot, plans, pantry deduction, customizations) works unchanged and each household can diverge. Nothing else needs seeding: the ingredient catalog is global, so copied `ingredientId`s resolve, image and source URLs are absolute CDN links, and Autopilot reads the household catalog on each request rather than an index built at creation. Order history is the source household's behaviour, not recipe content, and would make a stranger's Menu show "ordered 5 times" badges and bias their recommendations; the field list is explicit so a future personal field isn't copied by default. Background rather than inline because ~460 recipes take about half a second locally and more against Atlas, and a copy failure must not leave a user unable to create a household; no job queue, because the copy is idempotent and the backfill command is the retry. The unique `{householdId, source, sourceRecipeId}` index settles a concurrent copy. The short hold is what lets iOS skip a "setting up" state: the app loads the Menu as soon as the household exists, and a copy that takes under a second is done by then; a slower one still finishes, and the Menu is pull-to-refresh |
| 488 | **Account deletion** is `DELETE /api/v1/me`, served by `internal/account`, which owns no collection: each module deletes its own documents through `PurgeHousehold` / `PurgeUser` methods on its Mongo store (via `mongodb.DeleteByID` / `UnsetID`), wired in `cmd/server/account.go`. A household the user was the **last member** of is deleted with all its data; a **shared** household is left, and when the user is its only admin the **longest-standing other member is promoted** instead of refusing. The user's ratings are deleted; their ID is unset on events and cook usage and pulled from notification `readBy`; sessions, device tokens, identities, and the user go last. `TestIntegrationAccountDeletion` seeds every indexed collection and fails for one not classified | Apple requires in-app deletion for TestFlight external review (5.1.1(v)). Promoting rather than blocking was the safer of the two: a block (the leave rule) could leave someone unable to delete their account when the other members are unresponsive, while promotion only hands admin to the person who has shared the household longest, which in a family household is what the members would do anyway, and the household never loses its admin (the promotion increments `adminCount` before the leaver's conditional decrement). Shared plans, pantry and recipes belong to the household, so leaving keeps them; the user's own ratings are personal and go, while events and cook usage are the household's history and only lose the user ID. No transactions: steps are idempotent and the user record is last, so a failure leaves an account that can sign in and delete again. Access tokens already issued live out their 15 minutes; checking user existence on every request was not worth a read per call. Sign in with Apple tokens are not revoked because the API never exchanges an authorization code and holds no Apple token; doing so needs a Sign in with Apple key and client secret, a follow-up if App Review asks |
| 489 | Autopilot **learns from feedback inside the provider, recomputed on every request, never stored**. DinnerOS turns `meal.swapped` (the meal swapped *out*), `meal.rejected`, `recipe.planned`/`recipe.unplanned` with origin `autopilot`, `recipe.viewed` (not on the plan), `recipe.rated`, and cooked/skipped events (with whether their week was busy) into generic interactions with a time; `baseline/learn.go` derives per-meal values and per-attribute affinities (cuisine, protein, meal category, cook-time band) with an 8-week half-life over 26 weeks, plus "often skipped on busy weeks". The learned signal is weighted 0.10 and may take back at most half of a meal's taste contribution; every component that moves a score is a reason, shown even when negative (at most 3 standard + learned/context reasons, 5 in all). A reset is an `autopilot.learning_reset` event whose time becomes `Input.LearningSince`; the list is `GET .../autopilot/learning` through an optional `LearningReporter` interface | Stored weights would need invalidating on every swap, cook, view, rating, and reset, and would make the provider stateful behind an interface meant to be replaceable by a remote service; recomputing is one indexed events read (run concurrently with the other reads) and a pass over a few thousand interactions, about 10 ms added to a 146 ms generate on a real 464-recipe catalog. Learning in the provider rather than the adapter keeps it with the scoring it adjusts and available to any tenant. The caps keep the household in charge: explicit likes and dislikes can be shrunk but never reversed, and hard constraints run before scoring. Showing negative learned reasons is the honest half of explainability: a meal picked despite being swapped out should say so. A reset event rather than a profile field avoids a write that would mark an unconfigured profile as set up, and events older than the 26-week window can't matter, so the reset is found in the same read. Reading only the swapped-out meal: the swapped-in one was the engine's choice, not the member's |
| 490 | Autopilot **context is a generic typed signal map** (`autopilot.Signals`: text or number values, on the week and each day) consumed by calculators in `baseline/context.go` that ignore unknown keys and values. DinnerOS computes `season` (from the household's time zone, southern zones reversed), US `holiday`/`holidayKind` (computed locally: feast, cookout, day off), and `orderDate` (the household's order day); weekday is the slot's day. Context is soft, weighted 0.15, and the strongest part is a reason; holidays and device signals add week messages | The doc's contract asked for context that degrades gracefully rather than weather-specific fields, and a map lets the phone, a tenant, or a later calculator add signals without a contract change. Holidays and seasons are pure date arithmetic, so no API, key, or network call can fail a proposal. Kinds rather than names keep the provider locale-free. The household's habits are already covered elsewhere (a Sunday smoker rule, the cook-time mix, pairing rules for garlic bread and Club crackers), so context rules stay small nudges |
| 491 | **Calendar busyness and weather are derived on the iPhone** (EventKit, WeatherKit with coarse location) and sent as validated, bounded per-day values with a generate request: `signals.days[] {day, busyness: free/some/busy, eveningFreeMinutes 1–1440, temperatureBand: cold/mild/hot, precipitation: none/rain/snow}`. They are stored on the proposal (`context.signals`) so swaps reuse them, and nowhere else. The server never fetches weather and never receives calendar events, forecasts, or a location. Server-side weather (Open-Meteo) was not built | The household has only a time zone, too coarse for a forecast, and collecting a location on the server for this is more data than the feature needs. The phone already has the calendar and a location it can use without sending either, so it sends the least that works: a busy flag, free minutes, and weather bands. Storing them on the proposal keeps a swap consistent with the week it's swapping in; not reusing them on regenerate means stale weather can't outlive the request that sent it. The iOS side is #493 |
| 492 | A **long-cook weekday rule prefers genuine long cooks**: on a day whose rule has `timeBand: long`, `timeFit` is +1 for a meal of 60 minutes or more or one the caller marks `Item.LongCook`, +0.5 for the rest of the long band, and −0.5 for a known shorter cook, whose `rule` value is also halved; unknown times are neutral. The adapter marks whole birds, shoulders, butts, bellies, ribs, brisket, and chuck roast as `LongCook`. `ModelVersion` → `baseline-2026.7` | The household's Sunday is "a longer meal: whole or large cuts of chicken or pork on the smoker", and Phase 11 filled it with a familiar 35-minute pork tenderloin: `timeBand: long` only allowed a long cook and gave the long band a small bonus, so a well-rated quick smoker meal matched the rule fully and won. Halving the rule credit and penalizing short cooks moves the gap to about 0.4, more than a regular's familiarity and rating, while staying soft so the day is still filled when nothing long is left. Recipe cards understate smoker times, so a whole-cut heuristic counts as a long cook alongside the 60-minute threshold; tenderloins, loins, and chops stay smoker-friendly but aren't long. |
| 493 | **The iPhone derives calendar and weather signals** in `AutopilotDeviceContext` behind provider protocols: EventKit full access for busy spans (4–8 pm in the household's time zone, overlaps merged, all-day, free, and declined events ignored; free ≥ 210 minutes `free`, < 90 `busy`), reduced-accuracy When In Use location plus WeatherKit's daily forecast (high < 55 °F `cold`, ≥ 85 °F `hot`; precipitation at ≥ 50% chance). Permission is asked after a one-time explainer the first time a member plans with Autopilot, never at launch or from Siri; each signal has a switch in Autopilot Preferences; anything denied or failing is left out. Apple Weather attribution shows in the review when a forecast shaped the week | Asking at launch, before the member has seen what Autopilot does with it, gets reflexive denials and reads as overreach; asking at the moment of use with a plain statement of what leaves the phone is both honest and more likely to be allowed. Protocols keep EventKit, CoreLocation, and WeatherKit out of unit tests. The daily high tracks how a day feels for dinner better than an average, and a 50% chance avoids planning soup around a 20% shower. Reduced accuracy is all a daily forecast needs. |
| 494 | **Siri uses App Shortcuts calling the existing stores** through `DinnerIntentActions` and a `DinnerIntentServices` protocol, with `AppDependencies.shared` so an intent that launches the app in the background sees the same session and stores. "Plan my dinners" plans this week Monday–Thursday and next week from Friday, opens the review (`AppIntentRouter`), and summarizes in one deterministic sentence; on-device Foundation Models may rephrase it only behind availability checks, a 3-second limit, and a one-sentence check. "What's for dinner tonight" reads the plan directly so it never moves the week on screen | Thin intents keep one planning path (including device signals and permission rules) rather than a second one for Siri, and the protocol makes the signed-out, no-household, not-allowed, not-set-up, finalized, skipped, and empty cases testable. Friday is when households plan the coming week, while earlier most of this week is still ahead. The model only rewords facts it's given and the plain sentence is always the fallback, so it can't invent meals or stall Siri. |
| 495 | **A Walmart hand-off is one handoff per household, week, and provider that later sends add to.** Opening Walmart again sends only the difference: new lines, and for a line whose package count went up, the extra packages; a stored line's `packages` is what its links put in the cart. With nothing new the API returns no links and the app opens nothing ("Everything is already in your Walmart cart"). A decrease or replaced product is reported (`removePackages`, "Remove 1 in the Walmart app"), never linked. The handoff is `active` under a partial unique index and carries a `revision`, so two members sending at once add each package once. It closes when the week is marked ordered (unmarking reopens it), when a member starts over (pending lines skipped), or when every line is confirmed or skipped; the next send starts a new one. "Send Again" drops one ingredient's pending lines. A confirmed line is never grown: extra packages go on a new line, so each pantry purchase still maps to one handoff line. An open handoff stored before this counts as current until the next send adopts it | Walmart's add-to-cart link adds quantities and DinnerOS can't read or clear the cart, so every "Open in Walmart" used to re-add the whole list: the owner sent 13 items, came back to add more, and doubled all 13. The only record of what's in the cart is what DinnerOS sent, so it has to live server-side on the handoff for every member's phone to agree. Keying it by week matches how the list, the order reminder, and "Mark as Ordered" already work, and marking ordered is the one moment the household tells us the cart was checked out. Growing pending lines keeps "Did you order these?" asking one question per product at the count in the cart, and adopting pre-existing handoffs keeps the cart that exposed the bug from being filled a third time |
| 496 | **Package counting estimates volume ↔ weight with a typical density, and a `per_week` line needs no package size.** `providers.CountPackagesFor` takes the line's name and category: a spoon or cup need against an ounce-weight package (or the reverse) is converted with a density from a short name table (honey 1.4 g/ml, oil 0.92, spices 0.5, fresh leafy herbs 0.15…) or the category default, and `coverageText` keeps the recipe's units ("1 × 18 oz covers 2 tbsp"). An extreme density (1.5 g/ml, or 0.1 the other way) is a check: when only the category is known and the extreme would buy more, the count stands but reads "about". Under `per_week` a product with no saved size is 1 package covering the week, unflagged. Every remaining size-related flag on the Shop tab opens the product on its package size | The owner's Shop tab was full of "Check amount: 2 tbsp doesn't convert to a 18 oz package" and "no package size is saved" on cilantro, garlic and ginger — warnings with obvious answers that nobody could act on. Row 163 refused to guess; this narrows that rule rather than reversing it. The estimate lives only in package counting: `ingredients.Convert` still refuses volume↔mass, so recipe scaling and pantry deduction are unchanged. A density can be wrong by tens of percent, which rarely changes a count of small needs against whole packages, and the extreme bound says when it might. Missing sizes under `per_week` were flagged only because the no-size check ran before coverage: a size can't change a count that is already "one package for the week". What still warns — cloves against ounces, a capped count, a missing size on a pantry item — is a real question, and the warning is now a button to the answer |
| 497 | **Pantry `low` puts a line on the list like `out`, and a default staple answers for its aliases.** `GroceryPantry` puts `low` and `out` items in `PantryStock.OutOfStock`, so a member's status always beats a recipe's staple hint; only `in_stock` excludes. A default staple is registered under every name in its alias list (the "Cooking Oil" staple under "vegetable oil"), resolved against the catalog like any key. The Shop tab's exclusions read "In your pantry" (`in_pantry`) and "Usually on hand" (`pantry_hint`) | Supersedes the `low` half of row 56. The owner marked Butter low and the Shop tab kept it out of the cart as "Probably have it": "low" was read as "some is at home", but a member marking it means "buy more", and usage tracking marks items low for exactly that reason. The pantry already listed Cooking Oil in stock while recipes asked for Vegetable Oil — the staple's alias list existed to link it to the catalog at creation, and matching now uses the same list, not a broader guess, because a wrong equivalence would silently drop something from the list. "Probably have it" claimed knowledge the app didn't have; the new wording says whether it came from the pantry or from the recipe |
| 498 | **"Did you order these?" is grouped by meal from each handoff line's `recipes`, with one confirm button.** Handoff and match lines carry the grocery item's recipe references (`recipes`, stored on the handoff line). The app's `OrderConfirmationGroups` puts a line under its meal when it's for one (plan order, with the recipe's photo and day from the week's plan when that week is loaded), in "For Several Meals" when it's for more than one, and in "Extras" when it's only for add-on recipes or none. A meal whose items are all checked folds to "4 of 4 ordered". The button reads "Ordered All 43" (sends `all`) or "Ordered 42 of 43" (checked lines, `skipRest`), beside "Not Yet"; with nothing checked it's disabled. Supersedes the two-button layout in row 204 | The owner found a flat list of 40-odd products hard to check against what he cooked. The grocery engine already knew each item's recipes, so the API passes them through instead of the app re-deriving them from the plan, which could have changed since the handoff. An item shared by several meals is listed once, because two checkboxes for one line would be two states for one purchase. Photos and days aren't stored on the handoff: they come from the plan when it's loaded and the sheet degrades to names without it. Handoffs stored before `recipes` existed show everything under Extras rather than guessing |
| 499 | **Confirming a Walmart order tracks leftovers, not everything.** A handoff line becomes a pantry purchase when it's counted by exact measure (`per_amount`), or when it's `per_week` (produce, dairy, bakery, deli) with a known package size, a need that converts to it (exactly or by density), and at least 25% left over; meat and seafood never do. Other lines are confirmed with `pantry: not_tracked` and no purchase, and the same week's match leaves them out as `ordered` ("Ordered this week"). Narrows row 166's one purchase per confirmed line | Every confirmed line went in the pantry `in_stock`, so last week's ground beef, milk, and cilantro stayed "In your pantry" and off the next week's list unless recipes happened to deduct them below the threshold, while the owner's actual leftovers (sour cream, soy sauce, spices) were the point. The package-count math already knows what a week needs against a package, so the rule reuses it instead of a shelf-life table. 25% separates a tub of sour cream bought for 2 tbsp from a pack of beef bought for the recipe; meat is excluded because leftovers go in the freezer or the trash, not on a shelf. `ordered` keeps the same week from sending an untracked line to the cart again without pretending it's in the pantry |
| 500 | **Pantry purchases of an in-stock item add up, and cooked recipes' volume ↔ weight deductions are estimated by density.** A purchase while the item is `in_stock` carries the estimated remainder (≥ 10% of the old start, in a convertible unit) into the new cycle and marks the closed segment `carried`, which the learned rate skips; `low` and `out` still assume used up. A recipe's spoons against a package tracked by weight convert with `providers.EstimateAmount` (the package-counting density table), rounded to thousandths and marked `estimated` on the cook line. Narrows row 98 (and the pantry half of row 496) for cooked deductions only | The owner expects a second bottle of soy sauce to make more, not to reset to one bottle; the old rule was right only for replacing something used up, which is what low and out mean, so those keep teaching the rate. A carried remainder is the estimate itself, so learning from it would be circular. Walmart sells sour cream, brown sugar, and spices by weight and recipes use spoons, so exact-only conversion meant they never counted down or ran low. A density can be off by tens of percent, which is an acceptable error for "about 30% left", and people's amounts still convert exactly or not at all |
| 501 | **Prices are optional integer US cents entered by members: per handoff line (whole line), per pantry purchase, per saved product (one package), and a week's order total.** They arrive by typing, or from screenshots of the Walmart app's order details read on the iPhone: Vision text recognition, Foundation Models structuring when available (kept only for items whose price is in the recognized text), and a deterministic parser always. Matching to the week's lines happens on the phone and a review screen confirms; only confirmed prices and the total are sent. `POST .../handoffs/{id}/prices` writes the line, its purchase, and the saved product's per-package price | Walmart's emails have no item prices and apps can't read Mail, but the order details screen does list them. Keeping OCR and matching on device means no image or receipt text reaches the server, which also keeps a public repo and a privacy page simple. The parser is the fallback and the check, so an on-device model that invents a price can't save it. The handoff line is the one record per bought product, so prices hang off it and fan out to the purchase and saved product instead of making the app keep three copies in step. Screenshots are never committed; tests use invented OCR text |
| 502 | **Weekly cost is derived on read: spent, used this week, stocked for later, cost per meal, and vs meal kit.** Used = per priced item the whole price (not tracked), or price × week's need ÷ bought (tracked, measured; capped by the pantry's remaining estimate), or the whole price (tracked, unmeasurable) + earlier pantry stock this week's cooked planned meals deducted, at its purchase's unit price + order total − item prices. Meals are the plan's non-add-on entries. The meal kit baseline is a household setting (`mealKit: {weeklyCents, meals}`, `household.update`). `GET .../weeks/{week}/cost`, `PUT .../spend`, and `GET .../savings` (up to 26 weeks) | The owner's $130 Walmart cart matched $130 of HelloFresh, but much of the cart outlasts the week, so the comparison has to split spend into what the week consumed and what it stocked. The plan's need is known at order time, so the card is meaningful before anything is cooked, and the pantry's estimate corrects it as meals are cooked. Every ambiguity resolves toward used, so savings are never overstated, and the summary says how many items have prices. Deriving on read means a late price or order total changes every figure consistently. The baseline lives on the household next to the order day because it describes the household, not a store |
| 503 | **A household's first day of the week (`weekStartsOn`, `sun`–`sat`) changes which dates a week key covers, not the key.** Plans, proposals, week contexts, skips, handoffs, ordered weeks, prices, and notifications stay keyed `YYYY-Www`. Week K with first day S starts at K's ISO Monday moved by 0…+3 days (Mon–Thu) or −3…−1 (Fri–Sun) (`planning.Week.StartOn/DateOn`, `WeekOfOn`); a day code's date is its weekday inside that range. Plans carry `FirstDay` from `planning.WeekStartSource` (the household), and every response date, "current week", cooked-event week, menu day order, order reminder window, and Autopilot context (season, holidays, order date, `Input.WeekStart` for dates and adjacent days) uses it. **Changing it moves meals, not dates:** before the new value is saved, `planning.Service.ChangeWeekStart` moves each scheduled entry whose date would change into the neighboring week (same ID, day, date; finalized plans and the 50-entry cap don't block it; unscheduled entries stay). Each move pushes into the destination (skipped when that ID is already there) before pulling from the source, and marks the moved copy `weekStartMark: "from>to"`, so a retry after a failure never moves an entry twice; the marks are cleared after the save | Every other week-scoped collection and every client URL already uses the ISO key, so re-keying storage (Sunday-dated keys, or a per-plan layout) would have meant migrating or double-reading all of them. Shifting the range by at most three days keeps at least four days of each ISO week under its old key, so only the days between the old and new first day change weeks — with Sunday-first, only Sunday meals move, one week later. The alternative of leaving entries in place and showing a week as parts of two plans would have made grocery lists, finalization, and Autopilot span documents. Moved meals keep their dates, which is the one thing a member would notice; their pending proposal, week context overrides, pairing grocery items, and handoffs stay with the old week, as history or as suggestions to regenerate. |
| 504 | **Existing households read `mon`; new households get `sun`; `/weekstart` moves the rest deliberately.** `weekStartsOn` is absent on households created before it and `Household.FirstDay()` reads that as `mon`, which is exactly how their plans were laid out, so deploying changes no date. `Create` stores `sun` (or a given day). `cmd/weekstart` (dry run unless `-apply`, `-all` or `-household`, `-to`) switches households that never chose through `households.Service.Update`, the same path as Household Settings, and never touches one that chose | Defaulting absent to Sunday would have shown every stored Sunday meal a week early until a migration ran. Doing the switch at server start would move production data on deploy with no dry run; a command matches `/seedstarter` and lets the owner run it between shops, since a moved Sunday meal of an already-ordered week lands on the next week's list. |
| 505 | **iOS keeps `ISOWeek` as the week key and takes the start day as a required argument for every date.** `ISOWeek.startDate` stays the ISO Monday used in URLs; `startDate(weekStartsOn:)`, `endDate`, `containing`, `current`, `rangeLabel`, `weekOf`, `PlanDay.date(in:weekStartsOn:)`, `title`, `PlanDay.week(startingOn:)`, and `Plan.entriesInDayOrder` all take a `PlanDay`, using the same −3…+3 shift as the API (decision 503). `Household.weekStartsOn` decodes leniently (missing or unknown → `.mon`, how an older server stored weeks). By Day, Your Meals, day pickers, "has this night passed", proposal rows, Shop and grocery list titles, Siri, and calendar/weather day dates follow it; generic Autopilot preference day lists stay Monday first. This supersedes decision 64's "ISO week containing now" | A required parameter makes the compiler find every Monday assumption, where a global default would have hidden the ones nobody remembered, and keeps Swift Testing's parallel tests off shared state. |
| 506 | **The start day lives on the week stores, and changing it re-activates them.** `PlanStore`, `MenuStore`, and `ShoppingStore` hold an observable `weekStartsOn` (Sunday before a household loads) set by `activate(householdID:timeZone:weekStartsOn:)`; views key `.task(id:)` on `Household.weekScope` (ID + start day). For the same household a new start day reloads the shown week, jumping to the new current week when the current week was shown, and Shop drops its week match, cost, and order state. Household Settings offers "Week Starts On" (Sunday, Monday, then Tuesday–Saturday) | It's how the stores already receive the time zone. The server moves meals between weeks on a change, so anything cached per week is stale and is reloaded rather than patched. |
| 507 | **Menu card photos size from the offered width alone** (`RecipePhotoFrame`, a `Layout`: height = width ÷ aspect ratio, ignoring the proposed height), with the placeholder, loaded image (`scaledToFill`), and fallback glyph drawn inside the same clipped box | `.aspectRatio(_, contentMode: .fit)` also honoured the height a lazy horizontal stack proposed from earlier cards, so a card with more rows under its photo got a shorter, narrower photo and neighbouring Your Meals cards showed different sizes. |
| 508 | **Screenshots of the Walmart *cart* are read by layout, not by reading order.** Vision's boxes are flipped to a top-left origin and each screenshot is offset by one screen (`RecognizedPiece`/`RecognizedRow`, `OrderTextLayout.rows`); a price drawn as big dollars with small raised cents is rejoined into one amount; every bare price votes for the side its nearest name sits on and the majority decides whether prices lead names (`OrderScreenshotParser.pricesComeFirst`); and `OrderTitleCleaner` keeps only the title, dropping unit prices above it and Subscribe / SNAP EBT / returns / Remove / Save for later / badges / the quantity stepper after it. An imported price stays **the whole line**, what the cart charges for the quantity in the stepper, like every other price (decision 501) | The cart prints the price *above* the name, so binding the price that follows a name gave every item its neighbour's price, and the first name a screenshot shows is often a card whose price scrolled off, which is why the side can't be decided from the first row. "$1" with a raised "26" reaches recognition as two pieces: joined with a space they parse as $126.00, which is how cents reached the review screen as whole dollars. Chrome rows read as names, so they were glued onto the title above them and matched nothing. Keeping the line total means a stepper the parser misreads changes only the quantity shown, never the money, and the week's cost adds prices the same way whoever typed them |
| 509 | **An amount written without a decimal point is read as cents when the screen writes its prices that way, and words inside a product photo aren't part of its name.** Recognition usually returns the cart's big dollars and small raised cents as one word, "$244" for $2.44 and "$64" for $0.64. `OrderScreenshotParser.priceStyle` picks `.raisedCents` for a whole read when two or more amounts carry no separator, or when reading them in full puts the items over the order's own total (`ParsedOrder.itemsFitTotal`); then every separator-less amount ends in its cents, whatever its length, while anything with a decimal point or a thousands comma — the estimated total, a unit price, a was-price — is left exactly as written. `OrderTextLayout.withoutProductPhotos` drops pieces that end before the column the text starts in, and a wrapped name only carries on when the next row starts at the same edge, right under it | The style is a property of the screen, not of one row: a screen that can print "$2.44" prints it everywhere, so a bare amount means the separator was drawn, not written, and "$64" is 64 cents on the same screen where "$244" is $2.44 — guessing per amount would read the cheap items as $64.00. The total is printed large with a real decimal point and covers every item plus fees, so items adding up past it is proof the read is a hundred times out, and it settles a screen with only one bare amount on it. Packaging is text too: "Sour Cream Original" off the tub shares a row with the name and can't be told from it by words alone, but it is never in the text column |
| 510 | **A sheet, cover, alert, or popover is attached to a screen's stable root, and the state that drives it is held there too — never inside a `List` section, a row, or a branch that store state can take away.** The Shop tab's cost card (`WeekCostSection`) now takes an `open: (WeekCostSheet) -> Void` and owns nothing; `ShopView` holds `@State weekCostSheet` and presents `WeekCostSheetView`, as it already did for `ChooseProductSheet`. `WeekCostCard.isVisible` is a pure rule so the disappearance is testable. Confirmation dialogs are the one exception and stay on the button that opens them, because on iOS 26 they are popovers that point at whatever they're attached to (decision from "Anchor the sign-out and leave confirmations to their buttons") | `WeekCostSection`'s body is `if isVisible { Section { … } .sheet(item: $sheet) }`, so the presentation was hosted by rows of a section that hides itself whenever the week has no cost and no handoff — which is every week before `loadWeekCost()` answers, after a week change (`clearWeekCost`), and on an API that answers `404`. `loadWeekCost()` also reads handoffs and cost in two separate round trips and assigns both, so the section is re-diffed twice per refresh. A sheet whose host is torn down while it animates in is dismissed and `item` is written back to `nil`: on the phone, "Import Prices from Order Screenshots…" opened and closed itself, and the second tap worked because the load had settled by then. Hoisting the state removes the whole class of failure instead of pinning the card's identity with `.id()`, which would only have hidden it. The card's `.onChange(of: mealKit)` moved to `ShopView` for the same reason: inside the branch it never ran for a week whose card wasn't drawn |
| 520 | **The meal-kit import queue is a Mongo collection claimed with one atomic find-and-modify, drained by a scheduler-run command, not a worker dyno.** `meal_kit_jobs` holds status, attempts, `availableAt`, a `leaseOwner`/`leaseExpiresAt` visibility timeout, and a resumable checkpoint; `ClaimJob` matches either a due `queued` job or a `running` job whose lease expired, and every later write carries `{status: running, leaseOwner: me}`. `/importmealkit` runs on Heroku Scheduler every 10 minutes, fetches at most 40 recipe pages per job, checkpoints, and exits | The app already runs one Eco web dyno and already operates Heroku Scheduler for `/sendreminders`; a long-running consumer would mean a second always-on dyno (and Eco dynos sleep) for work that is bursty and deliberately slow, and a Redis queue would be an add-on to operate for the same job. MongoDB's single-document find-and-modify already gives exactly-once claiming, and the lease makes a killed dyno recoverable: the job is taken over from its checkpoint instead of re-fetching. The cost is up to one scheduler interval of latency, which is invisible against an import that spans several runs |
| 521 | **A meal-kit account is stored as session and refresh tokens under envelope encryption, never as a password and never as a hash.** `PUT .../meal-kit/{source}/link` exchanges the credentials for tokens in one call; a random per-link data key seals the tokens and `RECIPE_IMPORT_ENCRYPTION_KEY` seals the data key. `MEAL_KIT_IMPORT_ENABLED=true` without that key stops the API and the worker at startup. Expiry pauses the job at `paused_auth` and asks the member to sign in again; unlink deletes the tokens and cancels every job | A background job has to replay a credential, so a hash — one-way by definition — cannot be what is stored; the honest version is reversible encryption of the least powerful credential that works, with the password never persisted at all. Envelope encryption keeps the key out of the database and makes rotation a re-wrap of small data keys rather than a re-encryption of every secret, and failing startup loudly means there is no code path that can write a token in the clear |
| 522 | **The import fetcher is conservative by policy: one request at a time, ≥ 2.5 s apart plus up to 40% jitter, 4 attempts with backoff, `Retry-After` honoured, a hard stop on 403, an honest `User-Agent`, and a real 40-page-per-run cap.** Only the account's own deliveries endpoint and the recipe pages it names are ever requested, every URL is checked against the recipe-page prefix before the request, and a response that is not the shape this build expects is a `ParseError` that fails the job with a readable message instead of writing a half-read recipe | This is someone else's service and the household's own data, not a crawl target: the politeness has to be in the code rather than in a comment, which is why the cap is enforced and a 403 stops the run instead of backing off into a retry. Naming ourselves in the `User-Agent` means they can ask us to stop. Treating every fetched page strictly as data — never as an instruction, never as a source of the next URL — is what keeps a compromised or changed page from steering the worker, and failing cleanly on a layout change keeps the library trustworthy |
