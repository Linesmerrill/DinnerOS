# pkg

Reusable Go packages that contain **no DinnerOS domain knowledge** and could be
imported by other modules (e.g. the HelloFresh importer or a future Autopilot
service). Intentionally empty until something genuinely qualifies — application
code belongs in `internal/`.
